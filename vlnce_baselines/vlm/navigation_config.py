"""
VLM导航系统配置常量
===================
统一管理导航系统中的常量配置，避免重复定义
"""

# 4方向配置（从环视中提取）
# 环视是逆时针TURN_LEFT，12步×30°=360°
DIRECTION_STEPS = [0, 3, 6, 9]  # 对应12步中的第0,3,6,9步

DIRECTION_NAMES = [
    "Front (0°)",      # 步骤0: 初始朝向
    "Left (90°)",      # 步骤3: 左转90°
    "Back (180°)",     # 步骤6: 后方
    "Right (270°)"     # 步骤9: 右方（或左转270°）
]

# 全景图配置：每个方向3张图像拼接成90°视角
# 环视：每次TURN_LEFT后拍摄，转12次（1-12）
# 逆时针旋转，每次TURN_LEFT 30°
# step 1 = 30°, step 2 = 60°, ..., step 12 = 360° (正前方)
# 拼接顺序：从左到右（左-中-右）
# 前方(0°): step-1(30°,左) + step-12(0°,中) + step-11(330°,右) = 前方90°
# 左侧(90°): step-4(120°,左) + step-3(90°,中) + step-2(60°,右) = 左侧90°
# 后方(180°): step-7(210°,左) + step-6(180°,中) + step-5(150°,右) = 后方90°
# 右侧(270°): step-10(300°,左) + step-9(270°,中) + step-8(240°,右) = 右侧90°
PANORAMA_CONFIG = [
    {"name": "Front (0°)", "steps": [1, 12, 11]},    # 左-中-右
    {"name": "Left (90°)", "steps": [4, 3, 2]},     # 左-中-右
    {"name": "Back (180°)", "steps": [7, 6, 5]},    # 左-中-右
    {"name": "Right (270°)", "steps": [10, 9, 8]}   # 左-中-右
]

# 动作映射
ACTION_MAPPING = {
    "STOP": 0,
    "MOVE_FORWARD": 1, 
    "TURN_LEFT": 2,
    "TURN_RIGHT": 3
}
