"""
VLM规划提示词模板
================
用于LLM高层规划的提示词模板
"""

# 初始规划提示词 - 在任务开始时生成第一个子任务
INITIAL_PLANNING_PROMPT = """**Role**: You are a VLN planning module. Use the observations and map to localize the start position, identify the first reachable task stage, and output precise navigation instructions. No manipulation.

**Global Task**: {instruction}

**Initial state**: You are at the task start. Follow the Global Task strictly from the beginning and complete only the first subtask.

# Inputs
**12 Views** (sampled every 30° around 360°; each RGB view HFOV is about 79°):
- **Obstacle distance**: nearest obstacle only. <0.5m=blocked | 0.5-1.0m=caution | >1.0m=passable
- **In-view distance labels**: when shown on the image, `Obstacle` and `Landmark` display distances in meters; use only the shown value.
**Map**: explored area + obstacles + current pose
- **Map colors**: White=unexplored | Black=obstacles | Green=safe floor | Dark red=trajectory | Red Arrow=you position

# Reasoning (4 Parts)

**1) 12-View Analysis (MUST analyze EACH IMAGE 1-12)**
**Format**: For each IMAGE, use `IMAGE# (Direction Angle°): likely [space]; NEAR: ...; FAR: ...; Obstacle: ...; Landmark: ...`, and omit any field not actually visible there.
**Distance reading**: `Obstacle` and `Landmark` refer to the distance shown on that IMAGE only; use the shown meters only and do not infer hidden values.
**Evidence order**: When present, analyze only visible evidence in this order: NEAR current/large objects + implied space; FAR objects/openings + implied space; obstacle shown distance + blocked/caution/passable; landmark name + shown distance. `Landmark` detections are only partial noisy cues, and some visible objects may be missing, mislabeled, or too close to display, so always judge from the RGB content itself first. Spatial-transition categories such as hallway / doorway / entrance / corridor should be inferred from scene layout, not relied on as landmark detections.
**No hallucination**: analyze each IMAGE separately. If an IMAGE only shows a near wall or close furniture, say only that. Do not write `none`, fill empty slots, invent spaces / FAR objects / landmarks, or mention landmark unless explicitly shown.
**Conclusion**: Identify from the 12-view content: Current Position Guess: [current space + NEAR landmarks + surrounding scene across adjacent views] | Reachable Far Area/Landmark: [which IMAGEs show reachable FAR spaces/landmarks/openings, what space/landmark each one leads to, and which task-relevant transition/route point each one may support] | Blocked: [which IMAGEs have obstacle distance <0.5m]

**2) Current Position + Global Task Goal + Task Chain**
1. **Current Start Position**: localize the start position from Part 1. State both **which space** and **where inside that space** you are now; a space name alone is not enough. Use nearby landmarks, walls/openings, furniture layout, and continuity across adjacent views first, then use the Global Task only to choose the most task-relevant wording among the observation-supported anchors. For `current_waypoint`, use the same two-layer format as the rest of the system: `"[space]'s [landmark]"`. Here `landmark` means a distinctive in-space cue, which can be an object or a local place cue. Prefer a common space type or task-mentioned / task-required space name plus a nearby distinctive landmark. If the task suggests a task-relevant local anchor and the current observations support it, prefer that task-relevant space/landmark wording. If the task does not specify such an anchor here, do not invent one. Prefer task-mentioned landmarks, route cues, or the clearest nearby anchor. If local surroundings change, update the landmark wording rather than reusing the same suffix.
2. **Global Task Goal**: state the global task goal only in the form "[space]'s [landmark]". Also state the nearby landmarks/local layout that would confirm true arrival there, especially if the final landmark may be partly hidden.
3. **Parse Full Task**: reason over the natural-language Global Task and break it into ordered fixed stages from the current start position to the global task goal. Each stage must use `"[space]'s [landmark]" -> "[space]'s [landmark]"`. Split at cross-space transitions; keep same-space pass by / through / around relations inside one stage. If the task means passing through one space before another space, keep those as ordered separate stages, for example current space -> intermediate space anchor, then intermediate space -> next space anchor; do not collapse two spaces into one current stage. If the task describes a same-space pass / with / by / around relation, keep that cue inside one stage and use that stage's final landmark as the destination. Use task words such as pass / through / enter / toward / after / then and the task's overall meaning to infer order, stage boundaries, which destination should be pursued now, and which mentioned target is still later. If later stages are not explicitly specified by the task, do not invent extra future spaces/landmarks beyond the minimum task-supported route cues needed to continue; keep later unfinished anchors minimal and task-supported.
4. **Task Progress start**: because this is the task start, nothing is completed yet unless the global task goal is already reached at the start. Write `task_progress` as task-ordered natural-language sub-instructions expanded from the Global Task, separated by commas, not arrows. Mark only the current task piece `(Current)`; later task pieces stay unmarked; do not use `(✓)` unless the global task goal is already reached at the start. Expand only what is supported by the Global Task; do not invent extra future landmarks/spaces that the task never indicates.
5. **Task Waypoint Chain**: output the ordered stage-anchor chain that matches those stages and the already-judged `task_progress`. The current/start node must also be a full `"[space]'s [landmark](Current)"`, followed by later stage nodes in the same `"[space]'s [landmark]"` form until Goal. Never output bare `Start`, `Current`, `WP#`, or space-only nodes. Preserve task order and do not skip intermediate space/landmark. Keep unfinished later nodes within task-supported anchors only; if the task does not explicitly specify a later space/landmark, do not speculate beyond the minimum task-supported route cue. `current_waypoint` and the `(Current)` node in `waypoint_chain` must refer to the same current place/anchor, not two different locations.
6. **Task Goal Arrival Check**: reason from (a) the current localized position, (b) the global task goal's expected local cues, (c) current 12-view nearby evidence, and (d) the waypoint chain / task progress state. Judge how much of the Global Task is already complete from the ordered stage sequence. If the task says to enter or pass through another space before reaching the goal, do not stop early just because a later landmark is visible. If you are not yet in the global task goal space, do not stop. Only set `global_task_finish=true` if these cues consistently show that the global task goal `"[space]'s [landmark]"` is already reached within about 1.5m or the 12 views clearly show arrival there; otherwise false.
**Order**: localize first, then global task goal and expected cues, then fixed stages, then task_progress, then waypoint_chain, then Task Goal Arrival Check.

**3) Subtask Destination + Direction + Subtask Instruction + Landmark**
1. In initial planning, use only the first task stage. From the current localized position, the Global Task's ordered spaces/landmarks/route points, Part 1's reachable FAR spaces/landmarks, and the known space transitions, identify the first subtask destination: the next task-relevant `"[space]'s [landmark]"` that should be reached now. Advance spaces / route points / space transitions one by one in order. Use natural-language task reasoning to decide what is current versus later, which destination the task is actually asking for now, and which mentioned target must wait: if the task first requires entering another space or passing through an intermediate space before another space, keep the destination on that current cross-space stage only; if the first stage is a same-space pass-by / through relation, keep its final landmark as the destination and treat the pass-by cue as route relation inside that same stage; do not jump to a later-stage target just because it is visible. The destination must be the next unfinished anchor after the current localized position, never the task initial position unless the Global Task explicitly says to go back there. If the task does not explicitly specify a later space/landmark, keep only the minimum task-supported anchor instead of inventing one. `next_waypoint` must name one single core landmark only; do not join multiple candidates with `/`, `|`, or similar separators.
2. Choose the direction from the 12 IMAGEs that best reaches that destination. Prefer where the destination itself appears, or the FAR space/landmark/opening that most directly and safely leads to the needed next space/route point. Prefer open directions with obstacle distance >1.0m, ideally >2.0m; avoid blocked/tight views, repeated branches, and directions that only keep or send you back to the task initial position unless clearly required. The chosen direction must match task order and the current -> next relation, not just the most open view.
3. Write one short immediate subtask instruction for that same first stage. It must directly match the chosen destination and direction, and describe only the nearest unfinished task piece. The instruction form must be chosen from the Global Task meaning: for a cross-space stage, keep the instruction only on entering/approaching that current stage destination; for a same-space pass-by / with / around relation, keep the cue + final destination together in one instruction and keep the destination as that stage's final landmark. Do not leak later-stage goals into the current stage. Prefer one of these concise forms when appropriate:
   - Direct approach / enter: `From [next_waypoint_direction] view, start, [move/enter/approach] toward [destination].`
   - Via visible cue / obstacle bypass: `From [next_waypoint_direction] view, start, [pass/go through/go around/cross] [visible cue], then [enter/approach/continue toward] [destination].`
4. Choose `subtask_landmark` only for that first stage. It is the most useful visible concrete landmark cue for executing the current subtask now. Keep it as consistent with the destination as possible: if the destination itself is visible or can be named directly as the execution cue, prefer the destination itself; otherwise use a necessary visible intermediate landmark; if none is useful, use an empty string.

**4) Plan**
1. **Short-term Plan**: explain why this destination, direction, and subtask instruction are the correct immediate plan. Summarize the next move, turn if any, short-term target, and landmark choice if any, and make sure they match. State why this is the current unfinished stage from the Global Task order rather than a later stage, and why other visible directions would be premature or return toward the start.
2. **Long-term Plan**: summarize the remaining stage order after this subtask as a forward plan. Keep task-completion reasoning active: if a doorway / hallway / entrance crossing would place you into the next required space, that cross-space stage can finish there and the following in-room stage becomes next; if that new space is also the global task goal space and the goal cues are already satisfied, the task can end.

**Sequential planning rule**:
- Output only the immediate next task stage/subtask for current-stage progress; do not plan stage +2/+3 before stage +1 is finished.

# Output (JSON only)

{{
    "reasoning": "<Follow the reasoning flow above exactly. Part 1 must analyze IMAGE1, IMAGE2, ... IMAGE12 separately without merging and conclude with Current Position Guess / Reachable Far Area/Landmark / Blocked. Part 2 must reason in order: current start position, global task goal + expected local cues, fixed stages, task_progress, waypoint_chain, and Task Goal Arrival Check by comparing current localized position + current 12-view local evidence + chain/progress state against the global task goal. Use the Global Task to choose the most task-relevant observation-supported current anchor, then use that anchor to build task_progress before waypoint_chain. Then continue with Parts 3-4, using task semantics to decide which destination and direction belong to the current unfinished stage.>",
    "current_waypoint": "<Current position in the unified two-layer form: [space]'s [landmark]. Here landmark means a distinctive in-space cue, which can be an object or a local place cue. Prefer a common or task-mentioned space name plus a nearby distinctive task-relevant landmark. If the task supports a task-relevant local anchor and the current observations support it, prefer that wording; if not, do not invent one. Do not keep reusing the same landmark suffix when the local surroundings have changed.>",
    "task_progress": "<Task-ordered natural-language sub-instructions expanded from the original Global Task, not waypoint arrows. Use commas to separate sub-instructions. Same-space pass-by/through relations stay in one task piece with the final local destination as that piece's endpoint; cross-space transitions are separate task pieces. In initial planning only the current task piece is (Current); later task pieces are unmarked unless already at goal. Expand only what is supported by the Global Task; do not invent extra future landmarks/spaces that the task never indicates>",
    "waypoint_chain": "<Ordered stage-anchor chain using one [space]'s [landmark] node per fixed stage endpoint and consistent with task_progress. The current/start node must also be a full [space]'s [landmark](Current), not a bare Start/Current/WP# label. It must refer to the same current place/anchor as current_waypoint. Keep later unfinished nodes within task-supported anchors only. Do not mark (✓) in initial planning unless already at goal>",
    "next_waypoint": "<[space]'s single landmark only. If the subtask target is the landmark itself, explicitly include that landmark word. Do not use multiple alternatives like `A/B` or `A|B`. For same-space pass-by/through stages, this must be that stage's final landmark; for cross-space stages, this must be the current stage's destination in task order. It must be the next unfinished anchor after the current localized position, not the task initial position unless the Global Task explicitly says to return there>",
    "next_waypoint_direction": "<IMAGE 1-12 only; must match the chosen task-aligned view>",
    "subtask_instruction": "<Exactly one short sentence in one of three forms: direct `From [next_waypoint_direction] view, start, [move/enter/approach] toward [destination].`, same-stage path `From [next_waypoint_direction] view, start, [pass/go through/go around/cross] [visible cue], then [enter/approach/continue toward] [destination].`, or arrival stop `From [next_waypoint_direction] view, start, stop at [destination].` Use the path form only when that cue-destination relation belongs to the same current task piece, and use the stop form only when the destination is already reached now>",
    "subtask_landmark": "<Single clear recognizable visible landmark phrase if useful; otherwise empty string. This is the visible landmark cue for the current subtask. Keep it as aligned with the destination as possible: if the subtask destination itself is a visible landmark target, prefer the destination itself. Prefer specific landmarks like bed/rug/chair/table/lamp, not broad spaces. NEVER use door/doorway/hallway/corridor>",
    "global_task_finish": "<true only if Task Goal Arrival Check reasons from current position + current waypoint chain/task progress + visible goal evidence that you have already reached the correct global task goal space and global task goal landmark; otherwise false>"
}}

#Examples (abbreviated):

## Ex1: Bathroom to Giraffi via exercise room
**Global Task**: Turn around, walk through the exercise room into the living room. Wait by the Giraffi.
**Obs:** IMAGE 4-6: exercise-room opening and exercise equipment. IMAGE 8-11: bathroom sink / vanity / toilet.

{{
    "reasoning": "1) 12-Views: IMAGE1(Front 0°): likely hallway/transition edge; NEAR bookshelf; Obstacle passable. IMAGE2(Left 30°): likely bathroom boundary; NEAR window and wall; Obstacle caution. IMAGE3(Left 60°): likely bathroom boundary; NEAR window; Obstacle caution. IMAGE4(Left 90°): likely exercise-room transition; FAR exercise equipment and opening; Obstacle caution; Landmark: exercise equipment 2.7m. IMAGE5(Left 120°): likely exercise room; FAR exercise equipment and deeper opening toward the living room; Obstacle passable; Landmark: exercise equipment 2.3m. IMAGE6(Left 150°): likely exercise-room entrance; NEAR door frame; FAR exercise-room interior; Obstacle caution. IMAGE7(Back 180°): likely bathroom; NEAR wooden door; Obstacle caution. IMAGE8(Right 150°): likely bathroom; NEAR vanity and sink; Obstacle caution. IMAGE9(Right 120°): likely bathroom; NEAR vanity and mirror; Obstacle caution. IMAGE10(Right 90°): likely bathroom; NEAR toilet and vanity; Obstacle caution. IMAGE11(Right 60°): likely bathroom; NEAR toilet and wall; Obstacle caution. IMAGE12(Right 30°): likely bathroom boundary; NEAR wall and bookshelf edge; Obstacle caution. Conclusion: Current Position Guess: bathroom doorway by the sink/vanity side, facing the exercise-room opening. Reachable Far Area/Landmark: IMAGE4-6 open toward the exercise room; IMAGE5 most clearly shows the exercise equipment and the onward route, so it best supports the next task transition. Blocked: none strictly <0.5m, but most bathroom-side views are tight and only the forward-left route is clearly usable. 2) Current Position + Global Task Goal + Task Chain: Current Start Position: from the sink/toilet/vanity evidence in IMAGE8-11 and the exercise-room opening in IMAGE4-6, the current start position is Bathroom's sink-side doorway, facing the exercise room. Global Task Goal: the global task goal is Living room's Giraffi, confirmed only by living-room local evidence around the Giraffi rather than bathroom fixtures or the exercise-room transition. Parse Full Task: Bathroom's doorway -> Exercise room's exercise equipment | Exercise room's exercise equipment -> Living room's entrance | Living room's entrance -> Living room's Giraffi. Task Progress start: Turn around and enter the exercise room toward the exercise equipment(Current), continue through the exercise room into the living room, wait by the Giraffi. Task Waypoint Chain: Bathroom's doorway(Current) -> Exercise room's exercise equipment -> Living room's entrance -> Living room's Giraffi(Goal). Task Goal Arrival Check: compare the current localized position, the expected living-room Giraffi cues, and the current chain/progress state against the global task goal. The current nearby evidence is still bathroom sink/toilet/vanity plus the exercise-room opening, and the current node/task piece is still the bathroom start stage, so the goal is not reached and global_task_finish=false. 3) Subtask Destination + Direction + Subtask Instruction + Landmark: the first-stage destination is Exercise room's exercise equipment because the task must advance into the next space first, and IMAGE4-6 show that reachable transition with IMAGE5 giving the clearest open route. IMAGE5 is the best direction because it most directly and safely enters the exercise room toward the first task-relevant landmark. The immediate instruction is to enter toward the exercise room's exercise equipment. The landmark is exercise equipment because the destination itself is a visible task-relevant landmark. 4) Plan: short-term plan is to turn toward IMAGE5 and enter the exercise room using the visible exercise equipment as the cue because that is still the current unfinished stage from the Global Task order. Long-term plan is to reach the exercise room first, then continue into the living room, and finally wait by the Giraffi.",
    "current_waypoint": "Bathroom's sink-side doorway",
    "task_progress": "Turn around and enter the exercise room toward the exercise equipment(Current), continue through the exercise room into the living room, wait by the Giraffi",
    "waypoint_chain": "Bathroom's sink-side doorway(Current)→Exercise room's exercise equipment→Living room's entrance→Living room's Giraffi(Goal)",
    "next_waypoint": "Exercise room's exercise equipment",
    "next_waypoint_direction": "IMAGE 5 (Left 120°)",
    "subtask_instruction": "From IMAGE 5 (Left 120deg) view, start, enter toward the exercise room's exercise equipment.",
    "subtask_landmark": "exercise equipment",
    "global_task_finish": false
}}

**Critical Rules**:
- **Planning priority**: finish the nearest unfinished stage first. In initial planning, finish only the first stage, preserve task space/landmark/route-point order, and do not skip intermediate spaces.
- **Reasoning discipline**: Part 1 must stay evidence-only and conclude with Current Position Guess / Reachable Far Area/Landmark / Blocked. Use Part 1 first to localize exactly where you are, then use the Global Task only to choose the most task-relevant observation-supported current anchor, then parse the natural-language Global Task into fixed stages, judge task completion from task order + current observations + current position, write `task_progress`, build `waypoint_chain`, and then judge Task Goal Arrival Check. When parsing stages, split cross-space transitions into ordered separate stages, but keep same-space pass/through/around cues inside one stage whose destination is its final landmark. For destination/direction/instruction, use current localized position + task order + reachable FAR evidence + known space transitions + task semantics to choose the next reachable non-backtracking destination and the matching direction.
- **Current waypoint naming**: `current_waypoint` must be tied to the actual current local evidence, not copied mechanically from earlier waypoints. Use the unified two-layer form `"[space]'s [landmark]"`, where landmark means a distinctive in-space cue. Prefer a common or task-mentioned space name, and choose a nearby distinctive landmark that is task-mentioned, task-relevant, or immediately beside the agent. If the task supports a task-relevant local anchor and the observations support it, prefer that task-relevant anchor wording; if not, do not invent one. When position/local surroundings change, the landmark part should also change so nearby waypoints stay distinguishable. `current_waypoint` and the `(Current)` node in `waypoint_chain` must refer to the same place.
- **Progress, stop, and output**: `task_progress` must be task-ordered natural-language sub-instructions separated by commas, not waypoint arrows, and it should be judged before `waypoint_chain`: completed pieces in front, current piece `(Current)`, later pieces unmarked unless already at goal. Determine task completion from the Global Task's natural-language order together with current observations and current position. If a task piece is already behind the current place, it is completed and should not remain current. Same-space pass-by / through relations stay inside one task piece and keep the final local destination as that piece's endpoint; cross-space transitions split into separate task pieces. If the current evidence already shows the agent has reached the required pass-by / enter anchor for the current piece and the next task-ordered destination is now the unfinished target, advance to that next stage instead of keeping the old pass-by piece current. Do not invent extra future spaces/landmarks that the task never indicates; use only task-supported later cues when building unfinished parts of `task_progress`, `waypoint_chain`, and destination. `waypoint_chain` must use the full `"[space]'s [landmark]"` form for every node, including the current/start node and the goal node; never output bare `Start`, `Current`, `WP#`, or space-only nodes. `next_waypoint` must stay in "[space]'s [landmark]" form and match the current stage only, with one single core landmark and no slash-separated alternatives. `subtask_landmark` is the visible landmark cue for the current subtask and should stay as aligned with the destination as possible: prefer the destination itself when visible; otherwise use a necessary visible intermediate landmark or empty string. The destination should be the next unfinished anchor, not the task initial position unless the task clearly requires returning there. Never use door/doorway/hallway/corridor. `subtask_instruction` must be one short sentence in the fixed output form below. "At entrance" means doorway.
"""


# 验证和重规划提示词 - 验证子任务完成并生成下一步规划
VERIFICATION_REPLANNING_PROMPT = """**Role**: You are a VLN verification and replanning module. Use the space structure, views, and maps to verify subtask completion, localize the current position, and plan the next subtask. No manipulation.
{verify_replan_prompt_notice}

**Global Task**: {instruction}

**Previous Subtask**:
- Destination: {subtask_destination}
- Instruction: {subtask_instruction}

**Space Structure**: {waypoint_summary}

# Inputs
**{verify_view_count} Views** (sampled every 30°; each RGB view HFOV is about 79°):
- **Obstacle distance**: nearest obstacle only. <0.5m=blocked | 0.5-1.0m=caution | >1.0m=passable
- **In-view distance labels**: when shown on the image, `Obstacle`, `Landmark`, and `Space Waypoint` all display distances in meters; use only the shown value.
- **Custom landmark bbox** (if present): current-view cue only; use shown name + distance/angle only as room/object evidence, not map memory or path-clearance proof
**Global Map**: explored area + obstacles + trajectory + current pose + space structure
- **Map colors**: White=unexplored | Black=obstacles | Green=safe | Dark red=trajectory | Red Arrow=you position | Colored regions + blue tags=space structure on Global

# Reasoning (5 Parts)

**1) {verify_view_count}-View Analysis (MUST analyze EACH PROVIDED IMAGE ONLY)**
**Format**: For each provided IMAGE, use `IMAGE# (Direction Angle°): likely [space]; NEAR: ...; FAR: ...; Obstacle: ...; Landmark: ...; Space Waypoint: ...`, and omit any field not actually visible there.
**Distance reading**: `Obstacle`, `Landmark`, and `Space Waypoint` refer to the distance shown on that IMAGE only; use the shown meters only and do not infer hidden values.
**Evidence order**: When present, analyze only visible evidence in this order: NEAR current/large objects + implied space; FAR objects/openings + implied space; obstacle shown distance + blocked/caution/passable; landmark name + shown distance; reachable space waypoint + shown distance if that IMAGE's white strip / in-view labels show one. `Landmark` detections are only partial noisy cues, and some visible objects may be missing, mislabeled, or too close to display, so always judge from the RGB content itself first. Spatial-transition categories such as hallway / doorway / entrance / corridor should be inferred from scene layout, not relied on as landmark detections.
**No hallucination**: analyze each provided IMAGE separately. If an IMAGE only shows a near wall or close furniture, say only that. Do not write `none`, fill empty slots, invent spaces / FAR objects / landmarks / space waypoint cues, or mention landmark / space waypoint unless explicitly shown.
**Conclusion**: Identify from the provided-view content: Current Position Guess: [current space + NEAR landmarks + surrounding scene across adjacent views] | Reachable Far Area/Landmark: [which IMAGEs show reachable FAR spaces/landmarks/openings, what space/landmark each one leads to, and which task-relevant transition/route point each one may support] | Blocked: [which IMAGEs have obstacle distance <0.5m]

**2) Map + Space Structure Analysis**
Use only the Map and Space Structure in this part.
1. **Identify Current Area**: read the current area from the map and space structure, but always cross-check it with Part 1 nearby evidence. If the map current area is `Unknown` or conflicts with nearby evidence, resolve it from the strongest consistent provided-view nearby evidence.
2. **Read Space Waypoint list first**: analyze each `Space WP#...` one by one before reading the chain. If direction/distance are shown, enumerate them around the agent in counterclockwise order; within a similar direction sector, mention nearer waypoint(s) before farther waypoint(s). For each waypoint, state: which space/area it belongs to, its landmark meaning from the waypoint text, its shown direction and distance, whether it is directly reachable now, and if not, which earlier/current waypoint it should be reached through. Treat each waypoint as a full `space + landmark` anchor, not just a space label. If a waypoint line is marked `TASK INITIAL POSITION`, treat it as the task start anchor.
3. **Then Read Space Waypoint Chain**: after the waypoint-by-waypoint scan, treat each chain node as a full space waypoint plus its landmark meaning, not a bare space/WP id. Use it to know which spaces/waypoints are already visited, which are current / next / behind, how the agent arrived here, and which visible openings would cause wrong-space entry, repetition, or backtracking. Prefer not to revisit already passed spaces, especially the waypoint marked `TASK INITIAL POSITION`, unless the Global Task clearly requires returning there.
4. **Then Read Map**: align the current pose, waypoint list, and chain on the map. Check where each space lies, which side/opening corresponds to which waypoint/space, and which visible transition is the task-aligned next move versus an old/wrong branch.
5. **Structure Conclusion**: conclude `Current space/area from structure | current/next/behind space waypoint(s) | reachable task-aligned space transition(s) | wrong-space / backtracking transition(s) to avoid`.

**3) Current Position + Global Task Goal + Task Chain**
1. **Current Position**: localize the current position from {verify_view_count}-View Analysis. State both **which space** and **where inside that space** you are now. Use nearby landmarks, walls/openings, furniture layout, and continuity across neighboring views first; map pose and space structure are support only. Use the Global Task only to choose the most task-relevant wording among the observation-supported anchors. For `current_waypoint`, use the same two-layer format as the rest of the system: `"[space]'s [landmark]"`. Here `landmark` means a distinctive in-space cue, which can be an object or a local place cue. Prefer a common space type or task-mentioned / task-required space name plus a nearby distinctive landmark. If the task suggests a task-relevant local anchor and the current observations support it, prefer that task-relevant space/landmark wording. If the task does not specify such an anchor here, do not invent one. Prefer task-mentioned landmarks, route cues, or the clearest nearby anchor. If local surroundings change, update the landmark wording rather than reusing the same suffix. If the current evidence already best matches a task-relevant anchor such as stairs area / sofa side / rug side / doorway side, `current_waypoint` must move forward to that actually reached anchor rather than stay on an earlier space exit or older location.
2. **Global Task Goal**: state the global task goal only in the form "[space]'s [landmark]". Also state the nearby landmarks/local layout that would confirm true arrival there, especially if the final landmark may be partly hidden.
3. **Parse Full Task**: reason over the natural-language Global Task and break it into ordered fixed stages from the current start position to the global task goal. Each stage must use `"[space]'s [landmark]" -> "[space]'s [landmark]"`. Split at cross-space transitions; keep same-space pass by / through / around relations inside one stage. If the task means passing through one space before another space, keep those as ordered separate stages, for example current space -> intermediate space anchor, then intermediate space -> next space anchor; do not collapse two spaces into one current stage. If the task describes a same-space pass / with / by / around relation, keep that cue inside one stage and use that stage's final landmark as the destination. Use task words such as pass / through / enter / toward / after / then and the task's overall meaning to infer order, stage boundaries, which destination should be pursued now, and which mentioned target is still later. If later stages are not explicitly specified by the task, do not invent extra future spaces/landmarks beyond the minimum task-supported route cues needed to continue.
4. **Task Progress marking**: write `task_progress` as task-ordered natural-language sub-instructions expanded from the Global Task, separated by commas, not arrows. Completed task pieces stay in front with `(✓)`, the current unfinished task piece is `(Current)` ONE only, and later unfinished task pieces remain after it unmarked. Judge completion from ordered task meaning plus current position/current waypoint plus current space evidence, not single landmark matches alone. If the current localized position already matches the endpoint/anchor reached after a task piece, mark that earlier piece `(✓)` and move `(Current)` to the next unfinished piece; do not keep an already-reached pass-by / toward / enter piece as current. For same-space pass-by pieces, the stage completes when the required pass-by relation is already satisfied and the stage's final destination anchor is reached or passed as required by the task. If the pass-by / enter anchor required by the current piece is already effectively reached and the next task-ordered destination is now the unfinished target, advance to that next stage instead of staying on the old piece. Everything before the matched current anchor is already completed. Expand only what is supported by the Global Task; do not invent extra future landmarks/spaces that the task never indicates.
5. **Task Waypoint Chain**: build the ordered stage-anchor chain and anchor it to the already-judged **Current Position** plus Part 2's current/next space-waypoint state. Every node, including the matched current node and the goal node, must stay in full `"[space]'s [landmark]"` form. The matching node must be `(Current)`; nodes before it are `(✓)`; nodes after it are unmarked; if the current node is already the global task goal, mark it `(Current, Goal)`. Never output bare `Current`, `Goal`, `WP#`, or space-only nodes. The matched current node must be the farthest task-ordered anchor already reached by the current evidence; do not keep `(Current)` on an earlier node if the agent is already at a later anchor such as stairs area, doorway side, or rug side. `current_waypoint` and the `(Current)` node in `waypoint_chain` must refer to the same current place/anchor. Build unfinished later nodes only from task-supported later anchors; if the task does not explicitly specify a later space/landmark, keep the minimum task-supported route cue and do not speculate. Use the Space Waypoint Chain to judge whether a cross-space stage has already finished, for example if a doorway / hallway / entrance has already been passed and the agent is now in the next required space.
6. **Task Goal Arrival Check**: reason from (a) current localized position, (b) expected global-task-goal local cues, (c) provided-view nearby evidence, and (d) structure / waypoint list / Space Waypoint Chain / task waypoint chain / task progress. Use the current node and goal node as complete space + landmark anchors when checking whether the goal is already reached. Reason how much of the Global Task is complete and whether the current stage should finish, advance, or stop. If the current localized position already shows that an intermediate stage endpoint is reached, advance the task state before choosing the next subtask. If the resulting current node already matches the global task goal, stop before planning another destination. If you are not yet in the global task goal space, do not stop. Only set `global_task_finish=true` if these cues consistently show that the global task goal `"[space]'s [landmark]"` is already reached within about 1.5m or the provided views clearly show arrival there; otherwise false.
**Order**: localize first, then space structure state, then global task goal and expected cues, then fixed stages, then task_progress, then waypoint_chain, then Task Goal Arrival Check.

**4) Subtask Destination + Direction + Subtask Instruction + Landmark**
1. Based on the current position, space structure conclusion, Space Waypoint Chain, waypoint chain, provided-view observations, visible space waypoint cues, and the full Global Task, identify the immediate next subtask destination: the nearest unfinished task-relevant space/landmark that should be advanced now. Advance spaces / route points / space transitions one by one in order. Use natural-language task reasoning plus the Space Waypoint Chain to judge whether the current stage is still unfinished or has already switched to the next stage, which destination the task currently requires, and which later-mentioned target must wait. If the current task piece is a same-space pass-by / through relation, keep its final landmark as the destination and treat the pass-by cue as route relation inside that stage; if the task moves through one space before another space, keep the destination on the current cross-space stage only and leave the later space for the next stage; if the task moves into another space, use the next-space entry/landmark for the current stage, not a later-stage target. If the current localized position already matches the current stage endpoint/anchor, do not keep `next_waypoint` on that already reached node; advance to the next unfinished stage. If the current evidence already shows the required pass-by / enter anchor is effectively completed and the next task-ordered destination is now visible/reachable, advance to that next stage instead of keeping the old pass-by stage. The destination must be the first unfinished anchor after the matched current anchor, not the task initial position unless the Global Task explicitly says to go back there. If the task does not explicitly specify a later space/landmark, keep only the minimum task-supported anchor instead of inventing one. `next_waypoint` must name one single core landmark only; do not join multiple candidates with `/`, `|`, or similar separators.
2. If the current subtask is unfinished, continue it; if it is finished, advance to the next unfinished stage. Do not skip a nearer unfinished stage. Preserve waypoint order and task relations such as pass-by / toward / enter / after / then. For example, if the task said to enter another space first and the Space Waypoint Chain plus current evidence show that doorway / hallway / entrance has already been passed and you are now in that required next space, mark that cross-space stage finished and move to the next stage; likewise, if a same-space route cue such as stairs area / sofa side / rug side is already reached, mark the earlier pass-by / toward piece complete and move forward instead of telling the agent to keep walking to that already reached cue. If that new space/place already satisfies the global task goal, stop.
3. Choose the direction from the provided IMAGEs plus the space structure that best reaches that destination. Judge it from where the destination itself appears, or which reachable FAR space/landmark/opening/space-waypoint view most directly and safely leads toward the needed next space/route point. Use Part 2 and the Space Waypoint Chain to decide which view leads into the **correct next space** and which open views would enter a wrong, repeated, or backtracking space. Prefer open directions with obstacle distance >1.0m, ideally >2.0m; avoid blocked/tight views and backtracking unless clearly required, especially routes that return to already visited places or the waypoint marked `TASK INITIAL POSITION`. The chosen direction must match the current unfinished stage from the Global Task order.
4. Write the next subtask instruction for only that nearest unfinished stage. It must directly match the chosen destination and direction. It must also match the same current -> next step implied by the Space Waypoint Chain and task waypoint chain. Choose the instruction form from the Global Task meaning, not just from local visibility: if the current task piece is a same-space pass-by / through / around relation, keep that cue + destination in one instruction and keep the destination as the stage's final landmark; if the task changes to another space, keep the instruction only on the current cross-space stage and leave later-stage targets for later. If the pass-by / enter requirement is already effectively completed and the task has advanced, do not keep emitting the old pass-by instruction; switch to the next stage's destination instead. Reflect the task relation inferred from the Global Task rather than a generic move command. Never instruct the agent to move toward the already reached current node unless the correct action is to stop there. Prefer one of these concise forms when appropriate:
   - Direct approach / enter: `From [next_waypoint_direction] view, start, [move/enter/approach] toward [destination].`
   - Via visible cue / obstacle bypass: `From [next_waypoint_direction] view, start, [pass/go through/go around/cross] [visible cue], then [enter/approach/continue toward] [destination].`
5. Choose the most relevant visible subtask landmark. Keep it as consistent with the destination as possible: if the destination itself is a visible landmark target, prefer the destination itself as `subtask_landmark`. Otherwise use a necessary visible intermediate landmark; if none is useful, use an empty string.

**5) Plan**
1. **Short-term Plan**: explain why this destination, direction, and subtask instruction are the correct immediate plan. Summarize the next move, turn if any, short-term target, and landmark choice if any, and make sure they match. State why this is the current unfinished stage from the Global Task order, why any already reached anchor has been marked complete, and why alternative visible openings/directions would be later-stage, wrong-space, repeated, or backtracking choices, especially if they lead toward the task initial position.
2. **Long-term Plan**: summarize the remaining stage order after this subtask as a forward plan. Keep task-completion reasoning active: if the current cross-space stage finishes upon entering the next required room, the next in-room stage should become current; if the current room/place already satisfies the global task goal cues, the plan is complete.

**Sequential planning rule**:
- If the current subtask is unfinished, continue it; only after completion can `next_waypoint` move to the next stage.
- If you are already inside the correct global task goal space and the global task goal landmark is within about 1.5m, or the provided views clearly show arrival there, STOP and set `global_task_finish=true`.

# Output (JSON only)

{{
    "reasoning": "<Follow the reasoning flow above exactly. Part 1 must analyze each provided IMAGE separately without merging, mention any shown Space Waypoint in that same IMAGE line with its shown distance, and conclude with Current Position Guess / Reachable Far Area/Landmark / Blocked. Part 2 must explicitly analyze the space structure in this order: identify current area, then enumerate each Space WP# one by one in counterclockwise order with its space + landmark meaning + shown direction/distance + direct/via reachability, then analyze the Space Waypoint Chain for current/next/behind/visited state, then align them on the map, and finally conclude the task-aligned versus wrong/backtracking transitions to avoid. Part 3 must reason in order: current position, global task goal + expected local cues, fixed stages, task_progress, waypoint_chain, and Task Goal Arrival Check by comparing current localized position + current provided-view local evidence + structure + waypoint list + Space Waypoint Chain + waypoint chain/task progress state against the global task goal. Use the Global Task to choose the most task-relevant observation-supported current anchor, advance the current node/current task piece to the farthest already reached task-ordered anchor, and only then let Parts 4-5 choose the next destination and direction. Then continue with Parts 4-5, using task semantics plus Part 2 and the Space Waypoint Chain to justify destination, direction, and whether the current stage should continue, advance, or stop.>",
    "current_waypoint": "<Current position in the unified two-layer form: [space]'s [landmark]. Here landmark means a distinctive in-space cue, which can be an object or a local place cue. Prefer a common or task-mentioned space name plus a nearby distinctive task-relevant landmark. If the task supports a task-relevant local anchor and the current observations support it, prefer that wording; if not, do not invent one. Do not keep reusing the same landmark suffix when the local surroundings have changed. If current evidence already matches a later task-relevant anchor, use that reached anchor instead of an earlier one.>",
    "task_progress": "<Task-ordered natural-language sub-instructions expanded from the original Global Task, not waypoint arrows. Use commas to separate sub-instructions. Same-space pass-by/through relations stay in one task piece with the final local destination as that piece's endpoint; cross-space transitions are separate task pieces. Completed task pieces stay in front with (✓), current task piece=(Current) ONE only, later task pieces after it are unmarked, and if current is already the goal use (Current, Goal). If a task piece's endpoint/anchor is already reached, that piece must be (✓) and the next unfinished piece becomes (Current). Expand only what is supported by the Global Task; do not invent extra future landmarks/spaces that the task never indicates. All task pieces (✓)=complete>",
    "waypoint_chain": "<Ordered stage-anchor chain using one [space]'s [landmark] node per fixed stage endpoint, anchored to the judged current position and consistent with task_progress. Every node, including current and goal, must stay in full [space]'s [landmark] form; never output bare Current/Goal/WP# labels. Nodes before current=(✓), the matched current node=(Current), nodes after current=unmarked, and if current is the goal use (Current, Goal). The matched current node must be the farthest task-ordered anchor already reached by the current evidence, it must refer to the same place/anchor as current_waypoint, and later unfinished nodes must stay within task-supported anchors only>",
    "next_waypoint": "<[space]'s single landmark only; if the subtask target is the landmark itself, explicitly include that landmark word. Do not use multiple alternatives like `A/B` or `A|B`. For same-space pass-by/through stages, this must be that stage's final landmark; for cross-space stages, this must be the current stage's destination in task order. It must be the first unfinished anchor after the matched current anchor, not the task initial position unless the Global Task explicitly says to return there>",
    "next_waypoint_direction": "<one provided IMAGE label only; must match the chosen task-aligned direction>",
    "subtask_instruction": "<Exactly one short sentence in one of three forms: direct `From [next_waypoint_direction] view, start, [move/enter/approach] toward [destination].`, same-stage path `From [next_waypoint_direction] view, start, [pass/go through/go around/cross] [visible cue], then [enter/approach/continue toward] [destination].`, or arrival stop `From [next_waypoint_direction] view, start, stop at [destination].` Use the path form only when that cue-destination relation belongs to the same current task piece, use the stop form only when the destination is already reached now, and do not tell the agent to move toward an already reached current node>",
    "subtask_landmark": "<Single clear recognizable visible landmark phrase if useful; otherwise empty string. This is the visible landmark cue for the current subtask. Keep it as aligned with the destination as possible: if the subtask destination itself is a visible landmark target, prefer the destination itself. Prefer specific landmarks like bed/rug/chair/table/lamp, not broad spaces. NEVER use door/doorway/hallway/corridor>",
    "global_task_finish": "<true only if Task Goal Arrival Check reasons from current position + Space Waypoint Chain + current waypoint chain/task progress + current provided-view local evidence + visible goal evidence that you have already reached the correct global task goal space and global task goal landmark; otherwise false>"
}}

# Examples (abbreviated):

## Ex1: Rug arrival
**Global Task**: Exit bedroom, turn left. Walk straight passing gray couch, stop at rug.
**Previous**: Navigate past gray couch toward rug
**Obs:** IMAGE 1: Rug <0.5m and a living-room space waypoint. IMAGE 10: Gray couch beside. IMAGE 7: Hallway behind with hallway space waypoint.

{{
    "reasoning": "1) 12-Views: IMAGE1(Front 0°): likely living room; NEAR rug; Obstacle passable; Space Waypoint: Living room - at rug 0.4m. IMAGE2(Right 30°): likely living room; NEAR rug edge; Obstacle passable. IMAGE3(Right 60°): likely living room; NEAR nearby furniture; Obstacle passable. IMAGE4(Right 90°): likely living room; NEAR nearby furniture; Obstacle passable. IMAGE5(Left 120°): likely living room; NEAR living-room furniture; Obstacle passable. IMAGE6(Left 150°): likely living room; NEAR living-room furniture; Obstacle passable. IMAGE7(Back 180°): likely hallway transition; FAR hallway opening; Obstacle passable; Space Waypoint: Hallway - entrance opening 2.2m. IMAGE8(Right 210°): likely living room; NEAR side wall and furniture; Obstacle passable. IMAGE9(Left 240°): likely living room; NEAR gray couch; Obstacle passable. IMAGE10(Left 270°): likely living room; NEAR gray couch; Obstacle passable. IMAGE11(Left 300°): likely living room; NEAR rug and couch area; Obstacle passable. IMAGE12(Left 330°): likely living room; NEAR rug edge; Obstacle passable. Conclusion: Current Position Guess: living room rug area beside the gray couch. Reachable Far Area/Landmark: IMAGE7 leads back toward the hallway opening only as an old-space route. Blocked: none critical. 2) Map + Space Structure Analysis: Identify Current Area: the current pose lies in the living room area, consistent with the rug-and-couch evidence in Part 1. Read Space Waypoint list first: the visible current living-room waypoint is the rug anchor in IMAGE1 at 0.4m and is directly reachable now; the hallway entrance waypoint is behind in IMAGE7 at 2.2m and is reachable only by backtracking through the old transition. Then Read Space Waypoint Chain: the chain shows Bedroom -> Hallway -> Living room, with the living-room rug side as current and the hallway branch behind. Then Read Map: the pose sits inside the living-room region at the rug/couch side, while the hallway lies behind the agent. Structure Conclusion: current space/area=living room rug zone | current space waypoint=living room rug anchor | behind space waypoint=hallway entrance opening | reachable task-aligned transition=none needed because already at goal | wrong/backtracking transition to avoid=hallway opening behind. 3) Current Position + Global Task Goal + Task Chain: Current Position: Living room's rug beside gray couch. Global Task Goal: Living room's rug, confirmed by the rug itself plus the nearby gray couch / living-room rug zone. Parse Full Task: Bedroom's entrance -> Hallway's entrance opening | Hallway's entrance opening -> Living room's gray couch | Living room's gray couch -> Living room's rug. Task Progress: Exit bedroom(✓), turn left(✓), walk straight passing gray couch(✓), stop at rug(Current, Goal). Task Waypoint Chain: Bedroom's entrance(✓) -> Hallway's entrance opening(✓) -> Living room's gray couch(✓) -> Living room's rug(Current, Goal). Task Goal Arrival Check: compare the current localized position, the expected rug-area cues, the current 12-view nearby evidence, and the structure/chain/progress state against the global task goal. The current node is already the goal node, the current task piece is already the final task piece, and the rug with its nearby couch context is directly beside the agent in the views, so the goal is satisfied now and global_task_finish=true. 4) Subtask Destination + Direction + Subtask Instruction + Landmark: the immediate destination remains Living room's rug because it is the global task goal and it is already reached. The structure analysis also says the hallway transition behind is old space and must not be used. IMAGE1 remains the reference direction because the rug and current living-room waypoint are directly present there. The instruction is to stop at the living room's rug, and the rug is the clearest visible landmark. 5) Plan: short-term plan is to stop at the current rug position with no further movement because the Global Task is already complete. Long-term plan is complete because all fixed stages are already finished.",
    "current_waypoint": "Living room's rug beside gray couch",
    "task_progress": "Exit bedroom(✓), turn left(✓), walk straight passing gray couch(✓), stop at rug(Current, Goal)",
    "waypoint_chain": "Bedroom's entrance(✓)→Hallway's entrance opening(✓)→Living room's gray couch(✓)→Living room's rug beside gray couch(Current, Goal)",
    "next_waypoint": "Living room's rug",
    "next_waypoint_direction": "IMAGE 1 (Front 0°)",
    "subtask_instruction": "From IMAGE 1 (Front 0°) view, start, stop at the living room's rug.",
    "subtask_landmark": "rug",
    "global_task_finish": true
}}

## Ex2: Hallway to bedroom
**Global Task**: Walk through hallway, then enter bedroom on left and go to bed.
**Previous**: Navigate through hallway
**Obs:** IMAGE 1: Hallway ahead 3.0m. IMAGE 5: Bedroom opening (~2.5m), bed inside, plus bedroom space waypoint. IMAGE 7: Kitchen behind with kitchen space waypoint.

{{
    "reasoning": "1) 12-Views: IMAGE1(Front 0°): likely hallway; FAR hallway ahead; Obstacle passable. IMAGE2(Right 30°): likely hallway; NEAR side wall; Obstacle passable. IMAGE3(Right 60°): likely hallway; NEAR side wall; Obstacle passable. IMAGE4(Right 90°): likely hallway; NEAR side wall; Obstacle passable. IMAGE5(Left 120°): likely bedroom transition; FAR bedroom opening and bed; Obstacle passable; Landmark: bed 2.6m; Space Waypoint: Bedroom - near bed 2.5m. IMAGE6(Left 150°): likely bedroom transition; FAR left-side opening; Obstacle passable. IMAGE7(Back 180°): likely kitchen transition; FAR kitchen-side opening; Obstacle passable; Space Waypoint: Kitchen - near counter 2.8m. IMAGE8(Right 210°): likely hallway; NEAR wall; Obstacle passable. IMAGE9(Left 240°): likely hallway; NEAR wall and doorframe; Obstacle passable. IMAGE10(Left 270°): likely hallway; NEAR side wall; Obstacle passable. IMAGE11(Left 300°): likely hallway; NEAR side wall; Obstacle passable. IMAGE12(Left 330°): likely hallway; FAR hallway continuation; Obstacle passable; Space Waypoint: Hallway - forward section 1.5m. Conclusion: Current Position Guess: hallway by the left-side bedroom opening. Reachable Far Area/Landmark: IMAGE5-6 lead into the bedroom and support the next bedroom-entry transition toward the bed, while IMAGE7 is an alternate but wrong-space branch. Blocked: no critical blocked direction on the left entry route. 2) Map + Space Structure Analysis: Identify Current Area: the map and nearby evidence both place the agent in the hallway area. Read Space Waypoint list first: Space WP#1 [Hallway - forward section] appears around IMAGE12 at 1.5m and is an already passed hallway anchor; Space WP#2 [Kitchen - near counter] appears around IMAGE7 at 2.8m and is reachable only through a wrong/backtracking branch; Space WP#3 [Bedroom - near bed] appears around IMAGE5 at 2.5m and is the next task-aligned waypoint reached through the left opening. Then Read Space Waypoint Chain: the chain shows the agent has advanced along the hallway to the bedroom-side opening, with bedroom next and kitchen behind as a different branch. Then Read Map: the pose sits inside the hallway region close to the bedroom-side boundary, the bedroom lies to the left/front-left, and the kitchen branch lies behind. Structure Conclusion: current space/area=hallway near left bedroom opening | current space waypoint=hallway bedroom-side opening | next task-aligned space waypoint=bedroom near bed on the left | wrong/backtracking space waypoint=kitchen near counter behind | reachable task-aligned transition=left bedroom entry via IMAGE5-6. 3) Current Position + Global Task Goal + Task Chain: Current Position: Hallway's left bedroom opening. Global Task Goal: Bedroom's bed, confirmed only by bedroom-local evidence around the bed rather than hallway walls plus an entry opening. Parse Full Task: Hallway's bedroom-side opening -> Bedroom's doorway | Bedroom's doorway -> Bedroom's bed. Task Progress: Walk through hallway(✓), enter bedroom on left(Current), go to bed. Task Waypoint Chain: Hallway's bedroom-side opening(Current) -> Bedroom's doorway -> Bedroom's bed(Goal). Task Goal Arrival Check: compare the current localized position, the expected bedroom-bed cues, the current nearby evidence, and the structure/waypoint/chain/progress state against the global task goal. The current area/node is already at the hallway anchor beside the correct bedroom opening, so the hallway-walking piece is complete, but the agent is still outside the final bedroom-bed node. Therefore the goal is not yet reached and global_task_finish=false. 4) Subtask Destination + Direction + Subtask Instruction + Landmark: the immediate next destination is Bedroom's doorway because it is the first unfinished stage destination after the already reached hallway bedroom-side opening, and the reachable FAR opening plus bedroom space-waypoint cue in IMAGE5 support that next task transition. The space structure also confirms the left bedroom waypoint is next and the kitchen-side opening behind is wrong for this stage. IMAGE5 is the best direction because it most directly and safely leads into the correct next space. The instruction is to enter toward the bedroom's doorway, and the visible bed is the most relevant concrete landmark for this stage. 5) Plan: short-term plan is to turn toward IMAGE5 and enter the bedroom doorway using the visible bed as the cue because the cross-space entry is now the current unfinished stage. Long-term plan is to finish this doorway transition first, then continue inside the bedroom to the bed.",
    "current_waypoint": "Hallway's left bedroom opening",
    "task_progress": "Walk through hallway(✓), enter bedroom on left(Current), go to bed",
    "waypoint_chain": "Hallway's left bedroom opening(Current)→Bedroom's doorway→Bedroom's bed(Goal)",
    "next_waypoint": "Bedroom's doorway",
    "next_waypoint_direction": "IMAGE 5 (Left 120°)",
    "subtask_instruction": "From IMAGE 5 (Left 120°) view, start, enter toward the bedroom's doorway.",
    "subtask_landmark": "bed",
    "global_task_finish": false
}}

**Critical Rules**:
- **Planning priority**: finish the current nearest unfinished stage before later ones. Preserve task space/landmark/route-point order, and do not skip intermediate spaces.
- **Reasoning discipline**: Part 1 must stay evidence-only and conclude with Current Position Guess / Reachable Far Area/Landmark / Blocked. Part 2 must explicitly analyze the space structure in order: identify current area, scan each `Space WP#` one by one in counterclockwise order with its space + landmark meaning + shown direction/distance + reachability, then read the Space Waypoint Chain, then align them on the map, and then conclude which visible/open directions are task-aligned versus wrong-space/backtracking. Use Part 1 first to localize exactly where you are, then use the Global Task to choose the most task-relevant observation-supported current anchor, then use Part 2 plus the natural-language Global Task to anchor `task_progress` before `waypoint_chain`, judge ordered task completion, and decide from task semantics whether the current stage should continue, advance, or stop and which direction actually serves that stage. When parsing stages, split cross-space transitions into ordered separate stages, but keep same-space pass/through/around cues inside one stage whose destination is its final landmark. If current evidence already places the agent at a later task-relevant anchor, move the current node/progress forward before planning.
- **Current waypoint naming**: `current_waypoint` must be tied to the actual current local evidence, not copied mechanically from earlier waypoints. Use the unified two-layer form `"[space]'s [landmark]"`, where landmark means a distinctive in-space cue. Prefer a common or task-mentioned space name, and choose a nearby distinctive landmark that is task-mentioned, task-relevant, or immediately beside the agent. If the task supports a task-relevant local anchor and the observations support it, prefer that task-relevant anchor wording; if not, do not invent one. When position/local surroundings change, the landmark part should also change so nearby waypoints stay distinguishable. `current_waypoint` and the `(Current)` node in `waypoint_chain` must refer to the same place.
- **Progress, stop, and output**: `task_progress` must be task-ordered natural-language sub-instructions separated by commas, not waypoint arrows, and it should be judged before `waypoint_chain`: completed pieces in front with `(✓)`, current piece `(Current)`, later pieces unmarked; if current is already the final task piece, use `(Current, Goal)`. Determine task completion from the Global Task's natural-language order together with current observations and current position. If a task piece is already behind the current place, it is completed and should not remain current. Same-space pass-by / through relations stay inside one task piece and keep the final local destination as that piece's endpoint; cross-space transitions split into separate task pieces. Do not invent extra future spaces/landmarks that the task never indicate; use only task-supported later cues when building unfinished parts of `task_progress`, `waypoint_chain`, and destination. Task Goal Arrival Check must compare current localized position + expected global-task-goal local cues + provided-view nearby evidence + structure + waypoint list + Space Waypoint Chain + waypoint chain/progress state against the global task goal. For current progress, arrival, next destination, next direction, and next instruction, always reason with the Space Waypoint Chain together with the current task stage/progress. If these comparisons show the global task goal area is already reached, stop. If the task required crossing a doorway / hallway / entrance and the Space Waypoint Chain plus current evidence show that crossing is already finished, advance to the next stage; if a same-space anchor such as stairs area is already reached, mark the earlier piece complete and move to the next stage rather than continuing toward that same anchor; if the pass-by / enter requirement of the current piece is already effectively satisfied and the next task-ordered destination is now the unfinished target, advance to that next stage instead of keeping the old pass-by instruction/destination; if the resulting current room/place already satisfies the global task goal cues, stop. `waypoint_chain` must use the full `"[space]'s [landmark]"` form for every node, including current and goal; never output bare `Current`, `Goal`, `Start`, `WP#`, or space-only nodes. `next_waypoint` must stay in "[space]'s [landmark]" form and match the current stage only, with one single core landmark and no slash-separated alternatives. `next_waypoint_direction` must match the task-aligned current/next space waypoint relation from Part 2 and reject wrong-space/backtracking openings even if they are open. `subtask_landmark` is the visible landmark cue for the current subtask and should stay as aligned with the destination as possible: prefer the destination itself when visible; otherwise use a necessary visible intermediate landmark or empty string. The destination should be the first unfinished anchor after the matched current anchor, not the task initial position unless the task clearly requires returning there. Never use door/doorway/hallway/corridor. `subtask_instruction` must be one short sentence in the fixed output form below and must not tell the agent to move toward an already reached current node. "At entrance" means doorway.
"""


def get_initial_planning_prompt(instruction: str, 
                               action_space: str) -> str:
    """
    获取初始规划提示词
    
    Args:
        instruction: 完整导航指令
        action_space: 动作空间描述
        
    Returns:
        格式化的提示词字符串
    """
    return INITIAL_PLANNING_PROMPT.format(
        instruction=instruction,
        action_space=action_space,
    )

def _get_verify_view_count(direction_names=None):
    provided_direction_names = [
        str(name).strip()
        for name in list(direction_names or [])
        if str(name or "").strip()
    ]
    view_count = len(provided_direction_names)
    return view_count if 0 < view_count < 12 else 12

def get_verification_replanning_prompt(instruction: str,
                                       subtask_destination: str,
                                       subtask_instruction: str,
                                       action_space: str,
                                       detected_landmarks: str = None,
                                       waypoint_summary: str = None,
                                       verify_replan_prompt_notice: str = None,
                                       direction_names: list = None) -> str:
    """
    获取验证和重规划提示词
    
    Args:
        instruction: 完整导航指令
        subtask_destination: 当前子任务目的地
        subtask_instruction: 当前子任务指令
        action_space: 动作空间描述
        detected_landmarks: 已检测到的landmark类别字符串
        waypoint_summary: 空间结构字符串
        verify_replan_prompt_notice: verify/replan 顶部附加提示
        direction_names: 本轮实际提供给模型的方向图名称列表
        
    Returns:
        格式化的提示词字符串
    """
    if not detected_landmarks:
        detected_landmarks = "No landmarks detected yet"
    if not waypoint_summary:
        waypoint_summary = "No space structure recorded yet"
    if verify_replan_prompt_notice:
        verify_replan_prompt_notice = f"**Stuck Notice**: {verify_replan_prompt_notice.strip()}"
    else:
        verify_replan_prompt_notice = ""
    verify_view_count = _get_verify_view_count(direction_names)
    
    return VERIFICATION_REPLANNING_PROMPT.format(
        instruction=instruction,
        subtask_destination=subtask_destination,
        subtask_instruction=subtask_instruction,
        action_space=action_space,
        detected_landmarks=detected_landmarks,
        waypoint_summary=waypoint_summary,
        verify_replan_prompt_notice=verify_replan_prompt_notice,
        verify_view_count=verify_view_count,
    )
