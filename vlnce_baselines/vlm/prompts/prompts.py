"""
VLM规划提示词模板
================
用于LLM高层规划的提示词模板
"""

from vlnce_baselines.config.core.params.thresholds import (
    ARRIVAL_NEAR_M,
    OBS_BLOCKED_M,
    OBS_OPEN_M,
    OBS_RISKY_M,
)


def _fmt_threshold_m(value: float) -> str:
    text = f"{float(value):.2f}"
    if text.endswith("00"):
        return f"{float(value):.1f}"
    return text.rstrip("0").rstrip(".")

# 初始规划提示词 - 在任务开始时生成第一个子任务
INITIAL_PLANNING_PROMPT = """**Role**: You are a VLN planning module. Use the views and map to localize the task start position, identify the first reachable task stage from the start, and output precise navigation instructions for that first stage only. No manipulation.

**Global Task**: {instruction}

**Initial state**: You are at the task start. Follow the Global Task from the beginning and complete only the first stage/subtask. In normal initial planning, assume zero task progress: the task-start anchor is current, the first-stage endpoint is not yet reached, and no intermediate stage is complete. The initial subtask must serve only the true first task-defined destination/anchor/space waypoint, never a later visible stage. Use only the real current `Global Task`, `12 Views`, and `Map`; examples never override current input.

# Inputs
**12 Views** (sampled every 30° around 360°; each RGB view HFOV is about 79°):
- **Obstacle distance**: nearest obstacle only. <{obs_blocked_m}m=blocked | {obs_blocked_m}-{obs_risky_m}m=caution | >{obs_risky_m}m=passable
- **In-view distance labels**: when shown, `Obstacle` and `Landmark` display meters; use only the shown value.
**Map**: explored area + obstacles + current pose
- **Map colors**: White=unexplored | Black=obstacles | Green=safe floor | Dark red=trajectory | Red Arrow=you position

# Reasoning (4 Parts)

**1) 12-View Analysis (MUST analyze EACH IMAGE 1-12)**
**Format**: For each IMAGE, use `IMAGE# (Direction Angle°): likely [space]; NEAR: ...; FAR: ...; Obstacle: ...; Landmark: ...`, omitting any field not visible there.
**Distance reading**: `Obstacle` and `Landmark` refer only to that IMAGE's shown value; do not infer hidden values.
**Near rule**: treat only cues within about {arrival_near_m}m as truly NEAR/current-position evidence. This is for localization/progress only, not auto-stop. Farther cues may support direction/future destination but do not prove arrival.
**Evidence order**: read each IMAGE in this order: NEAR current/large objects + implied space; FAR objects/openings + implied space; obstacle distance + blocked/caution/passable; landmark + shown distance. Judge RGB/layout first; labels/distances are support only. If one cue conflicts with the stronger multi-view pattern, downweight it. Use all 12 views plus layout, openings, furniture relations, and adjacent-view consistency to decide current space, reachable spaces, and likely next direction. Use NEAR evidence first to decide whether you are in a room interior, hallway, landing, or other transition/outside area. A room visible through an opening does not make the current space that room. In stair scenes, infer upstairs/downstairs and top/run/bottom/off-stairs from geometry and adjacent views, not landmark detections.
**No hallucination**: analyze each IMAGE separately. If an IMAGE shows only a wall or nearby furniture, say only that. Do not write `none`, fill empty slots, invent spaces/FAR objects/landmarks, or mention landmark unless explicitly shown.
**Conclusion**: Identify from the 12-view content: Current Position Guess: [current space + NEAR landmarks + adjacent-view context] | Reachable Far Area/Landmark: [which IMAGEs show reachable FAR spaces/landmarks/openings, what each leads to, and which task-relevant transition/route point it may support] | Destination-Related Direction Guess: [which IMAGE/view most likely points toward the next task-relevant space or space landmark, and why] | Blocked: [which IMAGEs have obstacle distance <{obs_blocked_m}m]

**2) Current Position + Global Task Goal + Task Chain**
1. **Current Start Position**: localize from Part 1. State both **which space contains you now** and **where you are relative to its boundary**. Use nearby layout/cues, never one label/distance alone. Decide the current space from nearby evidence first: use a room name only if nearby evidence places you inside it; otherwise use the transition space instead of a farther room visible ahead. For `current_waypoint`, use `"[space] - [landmark1 / landmark2 / landmark3]"`, prefer concrete local cues, and use task wording only when observations truly match. In initial planning, this is the task-start anchor; do not move it to the first-stage endpoint or any later anchor before movement. Resolve this strict current anchor first: all later `task_progress`, `waypoint_chain`, and direction choices must follow it. `current_waypoint`, `task_progress`, and `waypoint_chain` must all match that same real start localization/progress; never infer them from a later visible landmark or invented stage/destination. Make it precise enough to distinguish segment start/middle/end, doorway side vs inside, local-object side, and stair top/run/bottom. At entrances, name the side you are actually on; do not name a farther room just because it is visible through an opening. If a task-named goal room/landmark is visible ahead but nearby evidence still places you at a doorway/hallway/threshold, keep `current_waypoint` at that threshold/outside anchor. In stairs, decide top vs bottom from geometry, but do not treat a later stair endpoint as already reached. If the task says to stop outside / at another entrance and views already match that outside anchor, use it only when that outside anchor is truly the task start.
2. **Global Task Goal**: state the goal only as `"[space]'s [landmark]"`. Also state the nearby landmarks/local layout that would confirm true arrival, especially if the final landmark may be partly hidden.
3. **Parse Full Task**: split the Global Task into ordered fixed stages from start to goal. Each stage must use `"[space]'s [landmark]" -> "[space]'s [landmark]"`. Split at cross-space transitions; keep same-space pass/through/around inside one stage whose destination is that stage's final landmark. Keep stages minimal and task-faithful, and preserve the Global Task's natural-language order and meaning as much as possible. Use task words such as pass / through / enter / toward / after / then to infer order, first-stage destination, and later targets. Keep motion cues such as turn left/right, turn around/back, or go straight as route constraints inside the stage they serve, not standalone stages unless the task explicitly says to face/stop there. Do not drop a still-unfinished route cue from a stage just because a later landmark in that same route is already visible. If the task means leave one space and stop outside / at the entrance of the next, use that outside/entrance anchor as the stage endpoint. For stair tasks, keep upstairs/downstairs explicit and infer top/bottom when needed. If later stages are not explicit, keep only the minimum task-supported unfinished anchors. In initial planning, the active task piece is always the **true first stage from task start**: task-start anchor -> first task-defined next anchor/space waypoint, meaning the first task-mentioned reachable anchor/landmark for the agent to go to now.
4. **Task Progress start**: at task start, nothing is complete. Write `task_progress` as task-ordered natural-language pieces from the Global Task, comma-separated, not arrows and not Space Waypoint Chain order. Keep each piece as close as possible to the original Global Task wording/order, especially route cues that are still part of the unfinished stage. In normal initial planning, `(Current)` must be the **first task stage**. Mark only that first-stage piece as `(Current)`; all later pieces stay unmarked; do not use `(✓)` for any intermediate piece. Judge each piece by comparing the strict current anchor to the ordered stage endpoints. If the current place does not yet prove a stage endpoint, keep that stage current and do not advance. Visible later rooms/doors/stairs do not mean earlier stages are complete. A stage endpoint counts as reached only when the localized space/space-type matches and the destination landmark/local anchor is near within about {arrival_near_m}m, or current views clearly show the required entrance/outside anchor. Do not mark a piece complete just because you are in the same broad space; completion requires the specific local endpoint. For stair anchors, generic `stairs` visibility is not enough: require the task's top/bottom/landing or on/off-stairs state. Keep turn cues inside the piece they serve, and do not rewrite away an unfinished cue just because you can already see a later landmark. Do not let `(Current)` lag behind `current_waypoint` or invent future landmarks/spaces.
5. **Task Waypoint Chain**: output the ordered **task-defined** stage-anchor chain matching those stages and `task_progress`, not the executed Space Waypoint Chain order. Every node, including the current/start node, must stay in full `"[space]'s [landmark]"` form, with the current/start node as `"[space]'s [landmark](Current)"`, followed by later task nodes until Goal. In initial planning, this `(Current)` node is the task-start anchor and the next node must be the first task-defined next anchor/space waypoint from the Global Task. Do not start the chain from the first-stage endpoint or any later anchor. Never output bare `Start`, `Current`, `WP#`, or space-only nodes. Preserve task order, do not skip intermediate anchors, and keep turn cues as route guidance unless the task explicitly makes the turned-to place itself the destination anchor. `waypoint_chain` must mirror the exact current localization and `task_progress`: `current_waypoint` and `(Current)` must refer to the same current anchor, with passed/unreached nodes clearly separated.
6. **Task Goal Arrival Check**: reason from (a) current localized position, (b) expected goal local cues, (c) current 12-view nearby evidence, and (d) waypoint chain / task progress state. In initial planning, judge goal arrival only from the real task-start localization; do not move the start anchor forward before movement. Do not decide arrival from one cue or shown distance alone; prefer the explanation that best matches the full task order and multi-view scene. Never advance goal state from guessed localization/progress/destination/chain state: if the exact goal anchor is not proved by current evidence, keep `global_task_finish=false`; if it is proved, stop immediately. The task goal is the exact target space/place named by the Global Task. If the task names another room of the same type, confirm it is that distinct target, not the current room's doorway/landmark. The task goal is not reached while any earlier task piece is still unfinished. For landmark/object goals, stop only when the current localized space matches the target space **and** the goal landmark/local anchor itself is near/current within about {arrival_near_m}m; this is the stop floor, but if the agent can safely get closer to the same correct goal landmark, prefer that closer stop and do not stop farther away when a closer safe approach is still available. Obstacle distance does not count. Seeing the goal farther away, through an opening, or from the wrong space does not count. For non-object goals, stop only when the current localized position itself is already inside the target space or at the exact entrance/outside anchor. For stair goals, generic `stairs` detection is not enough: confirm the required top/bottom/landing or on/off-stairs anchor. If the task says to enter or pass through another space first, do not stop early just because a later landmark is visible. Set `global_task_finish=true` only if the task-start location itself already matches the exact goal anchor.
**Order**: localize first, then global task goal and expected cues, then fixed stages, then `task_progress`, then `waypoint_chain`, then Task Goal Arrival Check. In reasoning, preserve the Global Task's stage order/meaning, judge completion from the strict current anchor rather than later visible landmarks, and stop only when the correct goal space/place is truly reached.

**3) Subtask Destination + Direction + Subtask Instruction + Landmark**
1. In initial planning, use only the first task stage. From the current localized position, the Global Task's ordered spaces/landmarks/route points, Part 1's reachable FAR evidence, and visible openings/transitions, identify the first subtask destination: the next task-relevant `"[space]'s [landmark]"` to reach now from the task start. This must be the first task-defined next anchor/space waypoint after the task-start anchor. Advance one task anchor at a time. If the first stage is cross-space, keep the destination on that stage; if it is same-space pass-by / through, keep the final landmark as the destination and keep the cue inside that stage. If a turn cue such as left/right/turn around/back/straight only guides the next destination, keep it as route guidance inside that same first stage, not a standalone target. The destination must be the next unfinished anchor after the strict current anchor, never the current or a passed anchor. In initial planning, the first-stage endpoint is always still unfinished, so choose it even if a later-stage room/landmark looks easier or is already visible. `next_waypoint` must stay on that first-stage endpoint and must not jump to any stage+1 anchor/room/doorway/landmark. If the task explicitly names the next-stage landmark/anchor, preserve it whenever possible instead of replacing it with a generic nearby cue. If the task requires another room of the same type, choose that other room's entry/landmark, not the current room's doorway. If you are already outside a space the task says to leave, do not choose it again unless the task explicitly requires re-entry. Prefer the nearest unfinished task-relevant destination already visible or directly supported by a visible opening/landmark. For stair stages, keep destination/instruction aligned with the task's vertical direction. If multiple openings exist, choose the one supporting the current stage. If a later space/landmark is not explicit, keep the minimum task-supported anchor. `next_waypoint` must name one single core landmark only.
2. Choose the direction from the 12 IMAGEs that best reaches that destination. First confirm the current position and the active first-stage destination from the task; then compare the actual content of all 12 views and pick the view whose visible space/landmark/opening best matches that destination. The initial direction must come from careful joint reasoning over the real task wording and the real 12-view content, not from openness alone or from a later visible stage. Prefer where the first-stage destination itself appears, or the FAR space/landmark/opening that most directly and safely leads to that first next anchor/space waypoint. When the first stage is still unfinished, direction must point toward that first-stage destination/cue, not a later-stage room/doorway/landmark even if it is visible. When several views are open, prefer the one whose visible space and landmark best match the active first stage rather than an older room exit or generic connector. If the first-stage task piece includes explicit guidance such as left / right / turn around / back / straight / until end, use that route cue first together with current position and visible evidence to choose the view, but still keep the destination on the first-stage endpoint. Do not spend the initial plan on any later stage until the first-stage endpoint is truly reached. If current evidence shows you are already outside a source room, continue toward the next required space/landmark and avoid re-entering that old room unless the task explicitly requires it. For stair stages, distinguish the upward stair side from the descending stair side using task wording plus scene geometry. If multiple openings are visible, prefer the one aligned with the current stage rather than an old/backtracking branch. Prefer open directions with obstacle distance >{obs_risky_m}m, ideally >{obs_open_m}m; avoid blocked/tight or backtracking directions unless clearly required. The chosen direction must serve the current unfinished task piece and its destination/landmark, not merely the most visible later landmark.
3. Write one short immediate subtask instruction for that same first stage. It must match the chosen destination and direction, and describe only the nearest unfinished task piece. For a cross-space stage, keep it only on entering/approaching that stage destination; for a same-space pass-by / with / around relation, keep the cue + final destination together and keep the destination as that stage's final landmark. If the first stage is still unfinished, `subtask_instruction` must stay on first-stage work and must not leak any stage+1 room/doorway/landmark. If the task gives explicit motion guidance such as turn left/right, go straight, or walk to the end, preserve it when the current route evidence still supports it. Do not leak later-stage goals into the current stage. Prefer one of these concise forms when appropriate:
   - Direct approach / enter: `From [next_waypoint_direction] view, start, [move/enter/approach] toward [destination].`
   - Via visible cue / obstacle bypass: `From [next_waypoint_direction] view, start, [pass/go through/go around/cross] [visible cue], then [enter/approach/continue toward] [destination].`
4. Choose `subtask_landmark` only for that first stage. It is the most useful visible cue for executing the current subtask now. Prefer the current-stage task-mentioned landmark itself if visible. If the current-stage destination itself names a concrete landmark, `subtask_landmark` should usually reuse that exact task word or a task-faithful synonym. If the first stage is still unfinished, `subtask_landmark` must support only that first-stage destination/cue and must not jump to any stage+1 room/doorway/landmark. Use another concrete cue only when that exact landmark is not visible or not useful yet. If the task does not name a useful current-stage landmark, infer one from the current-stage destination space + current 12-view evidence and choose a necessary visible cue for that stage. Output the landmark phrase itself, not the full destination string and not a `space's landmark` rewrite, unless the task itself uses a compound landmark phrase. Do not use a broad space type / room name / generic area label, and do not use an unrelated nearby object when a task-mentioned current-stage landmark is available. If no useful visible cue is needed, use an empty string.

**4) Plan**
1. **Short-term Plan**: explain why this destination, direction, and subtask instruction are the correct immediate plan. Summarize the next move, turn if any, short-term target, and landmark choice if any, and make sure they match. State why this is the current unfinished **first** stage from the Global Task order, not a later stage, and why other visible directions would be premature or backtracking.
2. **Long-term Plan**: summarize the remaining stage order after this subtask as a forward plan. If a connector crossing places you into the next required space, that cross-space stage can finish there and the following in-room stage becomes next; if that new space already satisfies the global task goal cues, the task can end.

**Sequential planning rule**:
- Output only the immediate next task stage/subtask; at task start this means only the true first stage endpoint after the task-start anchor. The start anchor is current, not the first endpoint. Judge completion and stopping from the strict current anchor, do not plan stage +2/+3 before stage +1 is finished, do not add extra stages, switch immediately once the current stage endpoint is truly reached, and stop only once the exact required target space/place is reached. For landmark goals, stop only when the correct goal space is reached and the goal landmark is near within about {arrival_near_m}m; if a closer safe stop is available, prefer it.

# Output (JSON only)

Return exactly one JSON object. Keep all Part 1-4 reasoning inside `"reasoning"`; never emit part titles like `"2) ..."` as extra keys. No extra keys, markdown, or prose. End at the final `}}`.

{{
    "reasoning": "<One compact string following Parts 1-4 exactly. Part 1: analyze IMAGE1-12 separately and conclude with Current Position Guess / Reachable Far Area/Landmark / Blocked; treat detections as noisy and never localize from labels/distances alone. Part 2: localize, set goal cues, fixed stages, `task_progress`, `waypoint_chain`, and Task Goal Arrival Check from current evidence + chain/progress state; preserve the Global Task's stage order/meaning and do not mark a stage complete from a later visible landmark alone. Parts 3-4: use task semantics, active motion cues, and connector choice to pick the current unfinished stage's destination/direction, reject backtracking, and explain why stop is or is not allowed yet. Keep all of this inside one JSON string.>",
    "current_waypoint": "<Space Waypoint style `[space] - [landmark1 / landmark2 / landmark3]`. Use observed nearby distinctive cues; decide the current space from NEAR evidence first, distinguishing room interior vs hallway/landing/outside side; prefer concrete nearby objects/local place cues over broad room labels; make the anchor locally precise enough to judge progress, such as segment start/middle/end, doorway side vs already inside, or top landing / stair run / bottom landing when supported by views; use task wording only when observations truly match; at entrances, name the space you are actually in now, not a farther room or farther goal landmark visible through the doorway; update the landmark wording as local surroundings change.>",
    "task_progress": "<Task-ordered natural-language pieces from the Global Task, comma-separated, not waypoint arrows or Space Waypoint Chain order. Same-space pass-by/through stays in one piece; cross-space transitions are separate pieces. Keep turn cues inside the piece they serve and preserve the Global Task's wording/order as much as possible; do not drop a still-unfinished route cue just because a later landmark is visible. In normal initial planning, nothing is complete: the first task piece is `(Current)`, no intermediate piece uses `(✓)`, and the subtask must still serve that same first piece. It must clearly match the exact current localization.>",
    "waypoint_chain": "<Ordered task-defined stage-anchor chain with full `[space]'s [landmark]` nodes only, not the executed Space Waypoint Chain order. The current/start node must also be a full node with `(Current)`, not bare Start/Current/WP#. In initial planning this `(Current)` node is the task-start anchor, never the first-stage endpoint, and the next node is the first task-defined next anchor after start. Turn cues are not standalone chain nodes unless the task explicitly makes them destination anchors. It must clearly match `current_waypoint` and `task_progress`.>",
    "next_waypoint": "<One `[space]'s [landmark]` only. No alternatives like `A/B` or `A|B`. In initial planning it must be the true first task-defined next anchor after the task-start/current anchor. The first stage is still unfinished, so it must stay on that first-stage endpoint and cannot jump to a later visible stage+1 anchor. It must serve the current unfinished task piece, not a later visible landmark from a not-yet-active stage.>",
    "next_waypoint_direction": "<IMAGE 1-12 only; must match the chosen task-aligned view>",
    "subtask_instruction": "<Exactly one short sentence in the fixed direct / same-stage path / arrival-stop form. Use the path form only when the cue and destination belong to the same current task piece; use the stop form only when the destination is already reached. In initial planning it must stay on first-stage work until the first-stage endpoint is reached.>",
    "subtask_landmark": "<One clear visible concrete landmark phrase if useful; otherwise empty. Prefer the current-stage task-mentioned landmark itself if visible; if the current-stage destination itself names a concrete landmark, usually reuse that exact task word or a task-faithful synonym. Otherwise infer a necessary concrete cue from the current-stage destination space + current views. Output the landmark phrase itself, not a `space's landmark` rewrite unless the task itself uses a compound landmark phrase. Never use a broad space type / room name, avoid unrelated nearby objects, and in initial planning do not jump to a stage+1 landmark while the first stage is still unfinished.>",
    "global_task_finish": "<true only if the exact global task goal anchor is already reached from current evidence + `task_progress` + `waypoint_chain`, and no earlier task piece remains unfinished: for landmark goals, the correct target space plus the goal landmark/local anchor near within about {arrival_near_m}m; this is the stop floor, but if a closer safe stop at the same landmark is available, prefer that; for non-object goals, the exact target space / entrance / outside anchor itself; otherwise false>"
}}

**Example note**: Examples below are only illustrative of format and reasoning. They are not current facts. Never copy their space names, landmarks, directions, or conclusions; always reason from the real current inputs.

# Examples (abbreviated):

## Ex1: Bathroom to Giraffi via exercise room
**Task**: Turn around, walk through the exercise room into the living room. Wait by the Giraffi.
**Obs:** IMAGE 4-6: exercise-room opening and exercise equipment. IMAGE 8-11: bathroom sink / vanity / toilet.

{{
    "reasoning": "1) 12-Views: IMAGE1 transition/bookshelf; IMAGE2-3 bathroom boundary/window; IMAGE4-6 exercise-room opening/interior with exercise equipment, IMAGE5 clearest; IMAGE8-11 bathroom sink/vanity/toilet. Conclusion: Current Position Guess: bathroom doorway on the exercise-room side with sink/vanity behind. Reachable Far Area/Landmark: IMAGE4-6 support the first move into the exercise room; the living room is later. Blocked: no critical block on IMAGE5. 2) Current Position + Global Task Goal + Task Chain: Current Start Position: Bathroom's exercise-room side doorway beside sink/vanity. Global Task Goal: Living room's Giraffi. Parse Full Task: Bathroom's exercise-room side doorway -> Exercise room's exercise equipment | Exercise room's exercise equipment -> Living room's entrance | Living room's entrance -> Living room's Giraffi. Task Progress start: Turn around and enter the exercise room toward the exercise equipment(Current), continue through the exercise room into the living room, wait by the Giraffi. Task Waypoint Chain: Bathroom's exercise-room side doorway(Current) -> Exercise room's exercise equipment -> Living room's entrance -> Living room's Giraffi(Goal). Task Goal Arrival Check: still in the bathroom, so global_task_finish=false. 3) Subtask Destination + Direction + Subtask Instruction + Landmark: the first-stage destination is Exercise room's exercise equipment. IMAGE5 is the clearest task-aligned entry. The instruction is to enter toward the exercise room's exercise equipment, and that landmark is directly useful. 4) Plan: short-term plan is to enter the exercise room. Long-term plan is exercise room, then living room, then the Giraffi.",
    "current_waypoint": "Bathroom - exercise-room doorway / sink / vanity",
    "task_progress": "Turn around and enter the exercise room toward the exercise equipment(Current), continue through the exercise room into the living room, wait by the Giraffi",
    "waypoint_chain": "Bathroom's exercise-room side doorway(Current)→Exercise room's exercise equipment→Living room's entrance→Living room's Giraffi(Goal)",
    "next_waypoint": "Exercise room's exercise equipment",
    "next_waypoint_direction": "IMAGE 5 (Left 120°)",
    "subtask_instruction": "From IMAGE 5 (Left 120deg) view, start, enter toward the exercise room's exercise equipment.",
    "subtask_landmark": "exercise equipment",
    "global_task_finish": false
}}

**Critical Rules**:
- **Reality priority**: use only the real current `Global Task`, `12 Views`, and `Map` as facts. Examples are demonstrations only; if any example conflicts with the current input, ignore it.
- **Initial-stage priority**: in normal initial planning, the task-start anchor is current, no intermediate task piece is complete, the first task piece is `(Current)`, `waypoint_chain` starts from that task-start anchor, and `next_waypoint` must be the first task-defined next anchor/space waypoint. Follow first-stage task cues such as left/right/turn around/back/straight only in service of that same first-stage destination. Do not jump to later visible targets, skip stages, invent extra stages, or move the current anchor forward before any movement.
- **Task-progress fidelity**: keep `task_progress` in the Global Task's original order and meaning as much as possible. Do not drop an unfinished turn / around-corner / pass-through / toward cue from the current piece just because a later landmark from that route is already visible.
- **First-stage consistency**: while the first stage is still unfinished, `next_waypoint`, chosen direction, `subtask_instruction`, and `subtask_landmark` must all stay on that same first-stage endpoint/cue. They must not jump ahead to a stage+1 room, doorway, or landmark.
- **Reasoning order**: keep Part 1 evidence-only, build the most self-consistent multi-view explanation, localize the strict current anchor first, then build task-defined stages, `task_progress`, `waypoint_chain`, and goal check. Keep same-space pass/through/around inside one stage, split cross-space transitions, preserve task motion cues, and reject backtracking.
- **Localization consistency**: know the exact current place first, then keep `current_waypoint`, `task_progress`, `waypoint_chain`, destination, and direction consistent with it. If the current place is not enough to prove a stage endpoint, do not advance the stage.
- **Direction/anchor discipline**: `current_waypoint` must follow actual local evidence and stay aligned with `(Current)` in `waypoint_chain`. First confirm the current anchor, then the active first-stage destination, then choose the direction whose real-view content best matches it. Do not choose direction from openness alone or from a later visible stage.
- **Landmark/stop discipline**: `subtask_landmark` should be the task-relevant concrete cue, not a broad room/space label. `waypoint_chain` is task-defined order, not executed path order. Stop only at the exact required target; for landmark goals, require the correct target space plus the goal landmark near/current within about {arrival_near_m}m as the stop floor, and prefer a closer safe stop if available. Never use obstacle distance as arrival proof.
"""


# 验证和重规划提示词 - 验证子任务完成并生成下一步规划
VERIFICATION_REPLANNING_PROMPT = """**Role**: You are a VLN verification and replanning module. Use the space structure, views, and maps to verify subtask completion, localize the current position, and plan the next subtask. No manipulation.{verify_replan_prompt_notice_block}

**Global Task**: {instruction}

**Previous Subtask**:
- Destination: {subtask_destination}
- Instruction: {subtask_instruction}
{previous_subtask_landmark_block}

**Space Structure**: {waypoint_summary}

**Reality priority**: Use only the real current `Global Task`, provided `Views`, `Space Structure`, `Global Map`, and `Previous Subtask` evidence as facts. Examples never override current input.

# Inputs
**{verify_view_count} Views** (sampled every 30°; each RGB view HFOV is about 79°):
- **Obstacle distance**: nearest obstacle only. <{obs_blocked_m}m=blocked | {obs_blocked_m}-{obs_risky_m}m=caution | >{obs_risky_m}m=passable
- **In-view distance labels**: when shown, `Obstacle`, `Landmark`, and `Space Waypoint` display meters; use only the shown value.
- **Custom landmark bbox** (if present): current-view cue only; use shown name + distance/angle only as room/object evidence, not map memory or path-clearance proof
**Global Map**: explored area + obstacles + trajectory + current pose + space structure
- **Map colors**: White=unexplored | Black=obstacles | Green=safe | Dark red=trajectory | Red Arrow=you position | Colored regions + blue tags=space structure on Global

# Reasoning (5 Parts)

**1) {verify_view_count}-View Analysis (MUST analyze EACH PROVIDED IMAGE ONLY)**
**Format**: For each provided IMAGE, use `IMAGE# (Direction Angle°): likely [space]; NEAR: ...; FAR: ...; Obstacle: ...; Landmark: ...; Space Waypoint: ...`, omitting any field not visible there.
**Distance reading**: `Obstacle`, `Landmark`, and `Space Waypoint` refer only to that IMAGE's shown value; do not infer hidden values.
**Near rule**: treat only cues within about {arrival_near_m}m as truly NEAR/current-position evidence. Farther cues may support direction/future destination but do not prove arrival.
**Evidence order**: read each IMAGE in this order: NEAR current/large objects + implied space; FAR objects/openings + implied space; obstacle distance + blocked/caution/passable; landmark + shown distance; reachable space waypoint + shown distance if that IMAGE explicitly shows one. Judge RGB/layout first; labels are support only. If one cue conflicts with the stronger multi-view pattern, treat it as weak/noisy. Use all provided directions plus layout, openings, furniture relations, adjacent-view consistency, and stair geometry to decide current space, reachable spaces, and likely next direction. Use NEAR evidence first to decide whether you are in a room interior, hallway, landing, or other transition/outside area. A room visible through an opening does not make the current space that room. Also decide whether you are on the stair run, at the top landing, at the bottom landing, or already off the stairs into the adjacent space.
**No hallucination**: analyze each provided IMAGE separately. If an IMAGE shows only a wall or nearby furniture, say only that. Do not write `none`, fill empty slots, invent spaces/FAR objects/landmarks/space waypoint cues, or mention landmark/space waypoint unless explicitly shown.
**Conclusion**: Identify from the provided views: Current Position Guess: [current space + NEAR landmarks + adjacent-view context] | Reachable Far Area/Landmark: [which IMAGEs show reachable FAR spaces/landmarks/openings, what each leads to, and which task-relevant transition/route point it may support] | Destination-Related Direction Guess: [which IMAGE/view most likely points toward the next task-relevant space or space landmark, and why] | Blocked: [which IMAGEs have obstacle distance <{obs_blocked_m}m]

**2) Map + Space Structure Analysis**
Use only the Map and Space Structure in this part.
1. **Identify Current Area**: read the current area from the map/space structure, then cross-check it with Part 1 nearby evidence. If the map says `Unknown` or conflicts with nearby evidence, resolve it from the strongest consistent nearby view evidence. If `Your Current Area` says you are near `INITIAL POSITION`, treat that as a warning to leave that waypoint neighborhood unless the task explicitly requires returning there.
2. **Read Space Waypoint list first**: analyze each `Space WP#...` before reading the chain. If direction/distance are shown, enumerate them counterclockwise; within a similar direction sector, mention nearer waypoint(s) before farther ones. For each waypoint, state its space/area, its local landmark meaning from the text, its shown direction/distance, whether it is directly reachable now, and if not, which earlier/current waypoint it should be reached through. Treat each waypoint as a full `space + landmark` anchor, not just a space label. If connected areas are shown, say which are task-aligned versus backtracking. If a line is marked `INITIAL POSITION`, treat it as the task start anchor.
3. **Then Read Space Waypoint Chain**: treat the chain as the true executed trajectory. Use it to know what is already visited, current/next/behind, how the agent arrived here, and which visible openings would cause wrong-space entry, repetition, or backtracking. Combine it with the Previous Subtask destination + landmark final observation to judge whether that old anchor was already reached, is now beside/behind/passed, and whether current progress should advance. Treat the nearest already reached waypoint behind/current in the chain as the effective last-position waypoint for progress judgment. Use that last-position waypoint plus the current local anchor to judge whether the agent has not started the present segment, has just entered it, is still in its middle, or has already reached/passed its far end. Make this concrete: decide whether you are at the start/middle/end of the current corridor, on the stair top/run/bottom/off-stairs, or inside/outside the current room/doorway. Treat `INITIAL POSITION` and the effective last-position waypoint as already visited anchors behind/current by default: use them to judge progress, not as the next route, unless current evidence clearly shows wrong-space entry, overshoot, or recovery is needed. If `(you have arrived now)` matches current views/structure, let it help move the current anchor forward; otherwise treat it as drift/overshoot evidence. Prefer not to revisit passed spaces, especially `INITIAL POSITION`, unless the task clearly requires it.
4. **Then Read Map**: align the current pose, waypoint list, and chain on the map. Check where each space lies, which side/opening corresponds to which waypoint/space, and which visible transition is the task-aligned next move versus an old or wrong branch.
5. **Structure Conclusion**: conclude `Current space/area from structure | current/next/behind space waypoint(s) | reachable task-aligned space transition(s) | wrong-space / backtracking transition(s) to avoid`.

**3) Current Position + Global Task Goal + Task Chain**
1. **Current Position**: localize from {verify_view_count}-View Analysis, then use task order, the Space Waypoint Chain, and the Previous Subtask destination + landmark final observation only as auxiliary evidence of what was already reached. First check whether the previous destination/landmark was truly reached; if current views/structure disagree, treat it only as overshoot/off-route evidence. Never localize from the previous-subtask landmark final distance alone. If the previous landmark says `(you have arrived now)`, use it only when it supports the **correct task-required space/anchor**. Decide the current space from nearby evidence first: use a room name only when nearby evidence places you inside it; otherwise use the transition space instead of a farther room visible ahead. For `current_waypoint`, use `"[space] - [landmark1 / landmark2 / landmark3]"`, prefer concrete local cues, and use task wording only when observations truly match. Resolve the strict current anchor before judging stage completion, progress, or destination, because `task_progress`, `waypoint_chain`, and next-stage choice must all follow it. `current_waypoint`, `task_progress`, and `waypoint_chain` must all match the same real current localization/progress; never infer them from a later visible landmark or invented stage/destination. Use where the chain says you came from plus the effective last-position waypoint behind/current to judge whether the current anchor is at the beginning, middle, or far end of the present corridor/stair/room segment, and whether the current stage has not started, is underway, is already completed, or has been overshot. The anchor must be precise enough to distinguish corridor start/middle/end, doorway outside vs already inside, room interior vs outside/threshold, local-object side, and stair top/run/bottom/off-stairs. At entrances, name the side you are actually on; do not name a farther room just because it is visible through an opening. If a task-named goal room/landmark is visible ahead but nearby evidence still places you at a doorway/hallway/threshold, keep `current_waypoint` at that threshold/outside anchor. In stairs, decide top vs bottom and whether climb/descent is finished from geometry. If the task says to stop outside / at another entrance and current views match that outside anchor, use it. `current_waypoint` must be the strict current anchor, not an earlier passed anchor or future target.
2. **Global Task Goal**: state the goal only as `"[space]'s [landmark]"`. Also state the nearby landmarks/local layout that would confirm true arrival, especially if the final landmark may be partly hidden.
3. **Parse Full Task**: split the Global Task into ordered fixed stages from the current start position to the goal. First extract route anchors as `"[space]'s [landmark]"`, then connect them into stages. Each stage must use `"[space]'s [landmark]" -> "[space]'s [landmark]"`. Split at cross-space transitions; keep same-space pass/through/around inside one stage whose destination is that stage's final landmark. Keep the stage list minimal and task-faithful, and preserve the Global Task's natural-language order and meaning as much as possible. Use task words such as pass / through / enter / toward / after / then to infer order, the current destination, and later targets. Keep motion cues such as turn left/right, go straight, or walk to the end then turn as route constraints inside the stage they serve, not standalone destinations unless the task explicitly says to face/stop there. Do not drop a still-unfinished route cue from a stage just because a later landmark in that same route is already visible. Use the strict current anchor to judge whether the next unfinished stage should keep moving inside the current space toward another local landmark, or should leave the current space toward the next required space/entrance anchor. If the task means leave one space and stop outside / at the entrance of the next, use that outside/entrance anchor as the stage endpoint. For stair tasks, keep upstairs/downstairs explicit and infer top/bottom when needed. If later stages are not explicit, keep only the minimum task-supported later anchors.
4. **Task Progress marking**: write `task_progress` as task-ordered natural-language pieces from the Global Task, comma-separated, not arrows and not Space Waypoint Chain order. Keep each piece as close as possible to the original Global Task wording/order, especially route cues that are still part of the unfinished stage. Completed pieces stay in front with `(✓)`, the current unfinished piece is `(Current)` ONE only, and later unfinished pieces remain after it unmarked. Judge completion from ordered task meaning + current position/current_waypoint + current space evidence + the Space Waypoint Chain, not single landmark matches alone. Compare the strict current anchor against ordered stage endpoints: if that anchor already reaches a stage endpoint, mark that piece complete and move `(Current)` forward immediately; if a later task landmark is only visible but the strict current anchor is still outside its required place, do not mark it complete or stop. Use the effective last-position waypoint behind/current to judge whether the current piece has not really started, has just started, is still mid-segment, is already near its endpoint, has already been passed, or has gone wrong-space. If the current place does not yet prove the stage endpoint, keep that stage current and do not advance. A stage endpoint counts as reached only when the localized space/space-type matches and the destination landmark/local anchor is near within about {arrival_near_m}m, or current views clearly show the required entrance/outside anchor. For landmark/object endpoints, use destination landmark/local-anchor nearness, not obstacle distance. Do not mark a piece complete just because you are in the same broad space; completion requires the specific local endpoint. For stair anchors, generic `stairs` visibility is not enough: require the task's top/bottom/landing or on/off-stairs state. Keep turn cues inside the piece they serve, and do not rewrite away an unfinished cue just because you can already see a later landmark. If a cross-space piece is already satisfied because current views show you have left the old space or reached the next entrance-side anchor, mark it complete immediately. Once a piece is truly satisfied, move to the next unfinished one. Do not let `(Current)` lag behind `current_waypoint` or invent future landmarks/spaces.
5. **Task Waypoint Chain**: build the ordered **task-defined** stage-anchor chain and anchor it to the already judged **Current Position** plus Part 2's current/next space-waypoint state. Do **not** copy the executed Space Waypoint Chain order into `waypoint_chain`; use it only to decide which task anchor is already reached. Every node, including current and goal, must stay in full `"[space]'s [landmark]"` form. The matched node must be `(Current)`; nodes before it are `(✓)`; nodes after it are unmarked; if the current node is already the global task goal, mark it `(Current, Goal)`. Never output bare `Current`, `Goal`, `WP#`, or space-only nodes. The matched current node must be the farthest task-ordered anchor already reached by current evidence, Previous Subtask evidence, and the Space Waypoint Chain. Turn cues are route guidance, not standalone chain nodes, unless the task explicitly makes them destination anchors. `waypoint_chain` must clearly mirror the exact current localization and `task_progress`: `current_waypoint` and `(Current)` in `waypoint_chain` must refer to the same place/anchor, and passed/current/unreached nodes must stay explicit. `(Current)` must be the strict current anchor; earlier nodes are passed, later nodes are unreached. Build later nodes only from task-supported later anchors.
6. **Task Goal Arrival Check**: reason from (a) current localized position, (b) expected goal local cues, (c) provided-view nearby evidence, and (d) structure / waypoint list / Space Waypoint Chain / task waypoint chain / task progress / previous-subtask landmark final evidence. Use the current node and goal node as complete space + landmark anchors. First compare the strict current anchor against the current stage endpoint and advance the task state if that endpoint is already reached; only then compare the updated strict current anchor against the final goal anchor. Do not decide arrival from the previous-subtask landmark final distance alone or from one current cue alone; prefer the explanation that best fits the full task order, multi-view scene, and structure. Never advance goal state from guessed localization/progress/destination/chain state: if the exact goal anchor is not proved by current task progress + strict current position + real evidence, keep `global_task_finish=false`; if it is proved, stop immediately. The task goal is the exact target space/place named by the Global Task. If the task names another room of the same type, confirm it is that distinct target, not the current room's doorway/landmark. If the Previous Subtask destination already equals the global goal and its landmark evidence says `(you have arrived now)`, use it as auxiliary evidence, but stop only if current views + structure also confirm the same goal anchor. The task goal is not reached while any earlier task piece is still unfinished. For landmark/object goals, stop only when the current localized space matches the target space **and** the goal landmark/local anchor itself is near/current within about {arrival_near_m}m; this is the stop floor, but if the agent can safely get closer to the same correct goal landmark, prefer that closer stop and do not stop farther away when a closer safe approach is still available. Obstacle distance does not count. Seeing the goal farther away, through an opening, or from the wrong space does not count. For non-object goals, stop only when the current localized position itself is already inside the target space or at the exact entrance/outside anchor. For stair goals, generic `stairs` detection is not enough: confirm the required top/bottom/landing or on/off-stairs anchor. If current views support the exact goal anchor, or the exact task-named outside/entrance stop point, stop immediately. If current localization already confirms an intermediate stage endpoint or entry into the next task-required space/landmark, advance the task state before choosing the next subtask; if no unfinished stage remains or the updated strict current node already matches the global goal, stop. Set `global_task_finish=true` only if these cues consistently show arrival.
**Order**: localize first, then space structure state, then global task goal and expected cues, then fixed stages, then `task_progress`, then `waypoint_chain`, then Task Goal Arrival Check. In reasoning, preserve the Global Task's stage order/meaning, use the strict current anchor plus effective last-position waypoint to judge whether the current stage is unfinished/completed/overshot, and stop only when the correct goal space/place is truly reached.

**4) Subtask Destination + Direction + Subtask Instruction + Landmark**
1. Based on current position, space structure conclusion, Space Waypoint Chain, waypoint chain, provided-view observations, visible space waypoint cues, and the Previous Subtask destination + landmark final distance/direction, identify the immediate next subtask destination: the nearest unfinished task-relevant space/landmark to advance now. Advance one task anchor at a time. Always start from the strict current anchor and choose the first unfinished anchor after it. First decide from the strict current anchor + task order whether the next unfinished stage should continue inside the current space toward another local landmark/waypoint, or should leave the current space toward the next required space/entrance anchor. This requires deciding where you are in the current stage: corridor start/middle/end, stair top/run/bottom/off-stairs, room inside/outside, doorway before/after crossing. If the current task piece is same-space pass-by / through, keep its final landmark as the destination; if the task moves through one space before another, keep the destination on the current cross-space stage only and leave the later space for the next stage; if the task moves into another space, use the next-space entry/landmark for the current stage, not a later-stage target. If a left/right/turn cue only tells how to reach that destination, keep it as a direction hint inside the same stage, not a standalone stage. If the current localized position already matches the current stage endpoint, do not keep `next_waypoint` there; advance immediately. If the task explicitly names the next-stage landmark/anchor, preserve it whenever possible instead of replacing it with a generic nearby cue. If the task requires a different room of the same type, the destination must be that other room's entry/landmark, not the current room's doorway. If current views + structure already confirm the next task-required space/landmark, use that to update `task_progress` and `waypoint_chain`; but if the task still requires leaving the current space and current evidence still places you inside it, keep the destination on that exit/entry stage. At entrances, if you are already outside a space the task said to leave, do not set `next_waypoint` back inside that old space. For stair stages, keep destination/instruction aligned with the task's vertical direction. The destination must be the first unfinished anchor after the strict current anchor, not the current node or a passed anchor. Unless current evidence shows wrong-space entry, drift, or recovery is needed, do not choose the effective last-position waypoint, `INITIAL POSITION`, or any already passed behind waypoint as the new destination. If the current stage is still unfinished, keep following that stage instead of turning back toward visited anchors just because they remain visible/open. Prefer the nearest unfinished task-relevant destination already visible or directly supported by a visible opening/landmark. If the current stage endpoint is not yet reached, keep working on that stage; once it is reached, switch immediately to the next unfinished stage or stop if the task is already satisfied. If current evidence shows wrong-space entry or overshoot, recover toward the nearest valid task-aligned unfinished anchor rather than wandering farther away. If multiple visible openings exist, choose the one aligned with the current stage. If a later space/landmark is not explicit, keep the minimum task-supported anchor. `next_waypoint` must name one single core landmark only. The chosen destination must follow the current unfinished task piece and its destination/landmark, not a later visible landmark from a not-yet-active stage.
2. If the current subtask is unfinished, continue it; if it is finished, advance to the next unfinished stage. Do not skip a nearer unfinished stage. Preserve waypoint order and task relations such as pass-by / toward / enter / after / then. If the Space Waypoint Chain plus current evidence show a required connector crossing is already finished, or a same-space local endpoint is already reached, mark that earlier piece complete and move forward immediately instead of repeating it. If the next required space/landmark is already confirmably visible/reached, advance to it immediately; if the task still says to enter/leave another space and current evidence still keeps you in the old space, do not advance yet. Be explicit about the stage state: unfinished and still approaching, already completed and ready to advance, or wrong-space/overshot and needing recovery. Do not keep an already reached stage current, do not stop early just because a later landmark is visible, and do not fall back to an already explored behind waypoint unless recovery is truly needed. If no unfinished task-supported stage remains, or the resulting space/place already satisfies the global task goal, stop immediately.
3. Choose the direction from the provided IMAGEs plus the space structure that best reaches that destination. First confirm the current position/current stage, then confirm from task logic whether the next unfinished stage should stay in the current space or leave it, then confirm the next-stage destination from the task, and finally compare the actual content of all provided views to choose the view whose visible space/landmark/opening best matches that destination. Judge it from where the destination itself appears, or which reachable FAR space/landmark/opening/space-waypoint view most directly and safely leads toward the needed next space/route point. If the next stage should leave the current space, prefer the view that leads out through the correct exit/connector toward the required next space; if the next stage stays inside the current space, prefer the view that best matches the needed in-space landmark/local endpoint. When several views are open, prefer the one whose visible space and landmark best match the active task stage rather than an older room exit or generic connector. If the current stage is still forward-progress and current evidence does not show wrong-space entry, overshoot, or off-route drift, prefer task-aligned front/side views that continue the present stage; use back-side views mainly for recovery. Do not turn back toward `INITIAL POSITION`, the effective last-position waypoint, or other visited-behind anchors just because they are open/visible. If the current unfinished task piece includes explicit action guidance such as left / right / straight / until end, use that route cue first with current progress, current position, and visible evidence; apply a turn cue only after the earlier stage it depends on is truly complete. Once the current stage endpoint is reached, prefer the task-cued side/sector and check whether that side actually shows the task-required next space/landmark. If current evidence shows you are already outside a source room or already at the correct entrance-side anchor, avoid re-entering that old room unless the task explicitly requires it. If the task says to enter another space or turn left/right, prefer the corresponding side direction, but still let task-aligned space/landmark evidence decide. For stair stages, distinguish the upward stair side from the descending stair side using task wording plus scene geometry. Use Part 2 and any connector linked-area info to choose the view leading into the **correct next space**, not a wrong/backtracking branch. The chosen direction must reflect the judged stage state: continue along the current segment if unfinished, switch to the next segment if completed, or recover toward the last valid task-aligned route if wrong-space/overshot. Prefer open directions with obstacle distance >{obs_risky_m}m, ideally >{obs_open_m}m; avoid blocked/tight or backtracking views unless clearly required. Direction must serve the current unfinished task piece and its destination/landmark, not a later visible landmark from a stage that is not yet active.
4. Write the next subtask instruction for only that nearest unfinished stage. It must match the chosen destination and direction, and the same current -> next step implied by the Space Waypoint Chain and task waypoint chain. Choose the instruction form from the Global Task meaning, not just local visibility: if the current task piece is same-space pass-by / through / around, keep that cue + destination in one instruction and keep the destination as the stage's final landmark; if the task changes to another space, keep the instruction only on the current cross-space stage and leave later-stage targets for later. If the pass-by / enter requirement is already effectively complete and the task has advanced, switch to the next stage's destination instead of repeating the old pass-by instruction. If the task gives explicit motion guidance such as turn left/right, go straight, or walk to the end, preserve that guidance when the current route evidence still supports it. Never instruct the agent toward an already reached current node unless the correct action is to stop there. Prefer one of these concise forms when appropriate:
   - Direct approach / enter: `From [next_waypoint_direction] view, start, [move/enter/approach] toward [destination].`
   - Via visible cue / obstacle bypass: `From [next_waypoint_direction] view, start, [pass/go through/go around/cross] [visible cue], then [enter/approach/continue toward] [destination].`
5. Choose the most relevant visible subtask landmark. Prefer the **next-stage task-mentioned landmark itself** if visible. If the next-stage destination itself names a concrete landmark, `subtask_landmark` should usually reuse that exact destination word or a task-faithful synonym. Use another concrete cue only when that exact landmark is not visible or not useful yet. If the task does not name a useful next-stage landmark, infer one from the next-stage destination space + current views and choose a necessary visible cue for that stage. Output the landmark phrase itself, not the full destination string and not a `space's landmark` rewrite, unless the task itself uses a compound landmark phrase. Do not use a broad space type / room name / generic area label, and do not use an unrelated nearby object when a task-mentioned next-stage landmark is available. If no useful visible cue is needed, use an empty string.

**5) Plan**
1. **Short-term Plan**: explain why this destination, direction, and subtask instruction are the correct immediate plan. Summarize the next move, turn if any, short-term target, and landmark choice if any, and make sure they match. State why this is the current unfinished stage from Global Task order, whether it should stay inside the current space or leave it for the next required space, why any already reached anchor has been marked complete, and why alternative visible openings/directions would be later-stage, wrong-space, or backtracking choices.
2. **Long-term Plan**: summarize the remaining stage order after this subtask as a forward plan. If the current cross-space stage finishes upon entering the next required room, the next in-room stage becomes current; if the current room/place already satisfies the global task goal cues, the plan is complete.

**Sequential planning rule**:
- If the current subtask is unfinished, continue it; only after completion can `next_waypoint` move to the next stage. Judge this from the strict current anchor versus the current stage endpoint.
- If the exact global task target space/place is not yet reached, do not stop just because a related landmark is visible. Stop only when the goal space/place is correct and all earlier task pieces are already satisfied. For landmark goals, stop only after the correct target space is reached and that goal landmark is near within about {arrival_near_m}m; if a closer safe stop is still available, prefer it, and if the goal landmark is still farther than that near range do not stop yet. If the strict current anchor already matches the goal anchor, STOP immediately and set `global_task_finish=true`.

# Output (JSON only)

Return exactly one JSON object. Keep all Part 1-5 reasoning inside `"reasoning"`; never emit part titles like `"2) ..."` as extra keys. No extra keys, markdown, or prose. End at the final `}}`.

{{
    "reasoning": "<One compact string following Parts 1-5 exactly. Part 1: analyze each provided IMAGE separately and conclude with Current Position Guess / Reachable Far Area/Landmark / Blocked. Part 2: analyze current area, each Space WP#, the Space Waypoint Chain, then the map. Part 3: reason through current position, goal cues, fixed stages, `task_progress`, `waypoint_chain`, and Task Goal Arrival Check; preserve the Global Task's stage order/meaning and use the strict current anchor plus effective last-position waypoint to judge whether the current stage is unfinished, completed, or overshot. Parts 4-5: justify destination, direction, and whether the stage should continue, advance, or stop, including why stop is or is not allowed yet. Keep everything inside one JSON string.>",
    "current_waypoint": "<Space Waypoint style `[space] - [landmark1 / landmark2 / landmark3]`. Use observed nearby distinctive cues; decide the current space from NEAR evidence first, distinguishing room interior vs hallway/landing/outside side; prefer concrete nearby objects/local place cues over broad room labels; make the anchor locally precise enough to judge progress, such as segment start/middle/end, doorway side vs already inside, or top landing / stair run / bottom landing when supported by views; use task wording only when observations truly match; at entrances, name the space you are actually in now, not a farther room or farther goal landmark visible through the doorway; update it as current evidence shows you have advanced.>",
    "task_progress": "<Task-ordered natural-language pieces from the original Global Task, comma-separated, not waypoint arrows or Space Waypoint Chain order. Same-space pass-by/through stays in one piece; cross-space transitions are separate pieces. Keep turn cues inside the piece they serve and preserve the Global Task's wording/order as much as possible; do not drop a still-unfinished route cue just because a later landmark is visible. Keep completed pieces in front, exactly one `(Current)` piece, and later pieces unmarked. It must clearly match the exact current localization.>",
    "waypoint_chain": "<Ordered task-defined stage-anchor chain with full `[space]'s [landmark]` nodes only, not the executed Space Waypoint Chain order. Current and goal must also stay in full form; never output bare Current/Goal/WP#. Turn cues are not standalone chain nodes unless the task explicitly makes them destination anchors. It must clearly match `current_waypoint` and `task_progress`.>",
    "next_waypoint": "<One `[space]'s [landmark]` only. No alternatives like `A/B` or `A|B`. It must be the first unfinished task anchor after the matched current anchor, not the current node or task initial position unless the task explicitly returns there. It must serve the current unfinished task piece, not a later visible landmark from a not-yet-active stage.>",
    "next_waypoint_direction": "<one provided IMAGE label only; must match the chosen task-aligned direction>",
    "subtask_instruction": "<Exactly one short sentence in the fixed direct / same-stage path / arrival-stop form. Use the path form only when the cue and destination belong to the same current task piece; use the stop form only when the destination is already reached.>",
    "subtask_landmark": "<One clear visible concrete landmark phrase if useful; otherwise empty. Prefer the next-stage task-mentioned landmark itself if visible; if the destination itself names a concrete landmark, usually reuse that exact task word or a task-faithful synonym. Otherwise infer a necessary concrete cue from the next-stage destination space + current views. Output the landmark phrase itself, not a `space's landmark` rewrite unless the task itself uses a compound landmark phrase. Never use a broad space type / room name, and avoid unrelated nearby objects when the task-mentioned next-stage landmark is available.>",
    "global_task_finish": "<true only if current evidence + chain/progress state show the exact global goal anchor is already reached and no earlier task piece remains unfinished: for landmark goals, the correct target space plus the goal landmark/local anchor near within about {arrival_near_m}m; this is the stop floor, but if a closer safe stop at the same landmark is available, prefer that; for non-object goals, the exact target space / entrance / outside anchor itself; otherwise false>"
}}

**Example note**: Examples below are only illustrative of format and reasoning. They are not current facts. Never copy their space names, landmarks, directions, or conclusions; always reason from the real current inputs.

# Examples (abbreviated):

## Ex1: Rug arrival
**Task**: Exit bedroom, turn left. Walk straight passing gray couch, stop at rug.
**Previous Subtask**: Navigate past gray couch toward rug
**Previous Landmark**: Landmark: [gray couch] (you have arrived now), 0.6m, Left 90deg
**Obs:** IMAGE 1: Rug <0.5m and a living-room space waypoint. IMAGE 10: Gray couch beside. IMAGE 7: Hallway behind with hallway space waypoint.

{{
    "reasoning": "1) 12-Views: IMAGE1 living-room rug; IMAGE2-6 and IMAGE8-12 living-room rug/couch area; IMAGE7 hallway opening behind. Conclusion: Current Position Guess: living-room rug area beside the gray couch. Reachable Far Area/Landmark: IMAGE7 only leads back to the hallway. Blocked: none critical. 2) Map + Space Structure Analysis: current area is the living-room rug zone; the rug waypoint is current and the hallway opening is behind/backtracking. 3) Current Position + Global Task Goal + Task Chain: Current Position: Living room's rug beside gray couch. Global Task Goal: Living room's rug. Parse Full Task: Bedroom's entrance -> Hallway's entrance opening | Hallway's entrance opening -> Living room's gray couch | Living room's gray couch -> Living room's rug. Task Progress: Exit bedroom(✓), turn left and walk straight passing gray couch(✓), stop at rug(Current, Goal). Task Waypoint Chain: Bedroom's entrance(✓) -> Hallway's entrance opening(✓) -> Living room's gray couch(✓) -> Living room's rug beside gray couch(Current, Goal). Task Goal Arrival Check: the current node already matches the goal, so global_task_finish=true. 4) Subtask Destination + Direction + Subtask Instruction + Landmark: the destination remains Living room's rug because it is already the current goal anchor. IMAGE1 stays the reference direction and the rug is the clearest landmark. 5) Plan: short-term plan is to stop now. Long-term plan is complete.",
    "current_waypoint": "Living room - rug / gray couch side",
    "task_progress": "Exit bedroom(✓), turn left and walk straight passing gray couch(✓), stop at rug(Current, Goal)",
    "waypoint_chain": "Bedroom's entrance(✓)→Hallway's entrance opening(✓)→Living room's gray couch(✓)→Living room's rug beside gray couch(Current, Goal)",
    "next_waypoint": "Living room's rug",
    "next_waypoint_direction": "IMAGE 1 (Front 0°)",
    "subtask_instruction": "From IMAGE 1 (Front 0°) view, start, stop at the living room's rug.",
    "subtask_landmark": "rug",
    "global_task_finish": true
}}

## Ex2: Hallway to bedroom
**Task**: Walk through hallway, then enter bedroom on left and go to bed.
**Previous Subtask**: Navigate through hallway
**Previous Landmark**: Landmark: [hallway forward section] (you have arrived now), 0.4m, Back 180deg
**Obs:** IMAGE 1: Hallway ahead 3.0m. IMAGE 5: Bedroom opening (~2.5m), bed inside, plus bedroom space waypoint. IMAGE 7: Kitchen behind with kitchen space waypoint.

{{
    "reasoning": "1) 12-Views: IMAGE1 hallway ahead; IMAGE2-4 and IMAGE8-12 hallway walls/continuation; IMAGE5-6 bedroom opening with bed; IMAGE7 kitchen opening behind. Conclusion: Current Position Guess: hallway by the left-side bedroom opening. Reachable Far Area/Landmark: IMAGE5-6 lead into the bedroom and support the next bedroom-entry transition; IMAGE7 is a wrong-space branch. Blocked: no critical block on the left route. 2) Map + Space Structure Analysis: current area is the hallway near the bedroom opening; bedroom is the next task-aligned waypoint and kitchen is behind/backtracking. 3) Current Position + Global Task Goal + Task Chain: Current Position: Hallway's left bedroom opening. Global Task Goal: Bedroom's bed. Parse Full Task: Hallway's left bedroom opening -> Bedroom's doorway | Bedroom's doorway -> Bedroom's bed. Task Progress: Walk through hallway(✓), enter bedroom on left(Current), go to bed. Task Waypoint Chain: Hallway's left bedroom opening(Current) -> Bedroom's doorway -> Bedroom's bed(Goal). Task Goal Arrival Check: the bed is visible deeper inside the bedroom, but the strict current anchor is still hallway-side and the bed is not yet a near/current goal anchor, so global_task_finish=false. 4) Subtask Destination + Direction + Subtask Instruction + Landmark: the immediate next destination is Bedroom's doorway. IMAGE5 is the clearest left entry into the correct next space; the kitchen opening behind is wrong. Because the current stage is the doorway-entry stage, the doorway itself is the most relevant landmark now. 5) Plan: short-term plan is to turn toward IMAGE5 and enter the bedroom doorway. Long-term plan is doorway, then bed.",
    "current_waypoint": "Hallway - left bedroom opening / wall side",
    "task_progress": "Walk through hallway(✓), enter bedroom on left(Current), go to bed",
    "waypoint_chain": "Hallway's left bedroom opening(Current)→Bedroom's doorway→Bedroom's bed(Goal)",
    "next_waypoint": "Bedroom's doorway",
    "next_waypoint_direction": "IMAGE 5 (Left 120°)",
    "subtask_instruction": "From IMAGE 5 (Left 120°) view, start, enter toward the bedroom's doorway.",
    "subtask_landmark": "doorway",
    "global_task_finish": false
}}

**Critical Rules**:
- **Reality priority**: use only the real current `Global Task`, provided `Views`, `Space Structure`, `Global Map`, and `Previous Subtask` evidence as facts. Examples are demonstrations only; if any example conflicts with the current input, ignore it.
- **Planning priority**: finish the current nearest unfinished stage first. Preserve task order, do not skip intermediate spaces, and do not invent extra stages.
- **Task-progress fidelity**: keep `task_progress` in the Global Task's original order and meaning as much as possible. Do not drop an unfinished turn / around-corner / pass-through / toward cue from the current piece just because a later landmark from that route is already visible.
- **Reasoning order**: keep Part 1 evidence-only, then analyze current area / each `Space WP#` / Space Waypoint Chain / map, build the most self-consistent explanation across current views, structure, task order, and history, localize the strict current anchor first, use the effective last-position waypoint to judge progress state, then decide `task_progress`, `waypoint_chain`, destination, and stop. Use Previous Subtask only as auxiliary evidence.
- **Localization consistency**: know the exact current place first, then keep `current_waypoint`, `task_progress`, `waypoint_chain`, destination, and direction consistent with it. If the current place is not enough to prove a stage endpoint, do not advance the stage.
- **Direction/turn discipline**: first confirm the current anchor, then whether the next unfinished stage stays in the current space or leaves it, then the active next-stage destination, then choose the direction whose real-view content best matches it. Do not choose direction from openness alone. Use task turn cues only after the earlier stage they depend on is truly complete. Do not lightly turn back toward `INITIAL POSITION`, the effective last-position waypoint, or other visited-behind anchors unless wrong-space entry, overshoot, or recovery is clearly supported.
- **Anchor/landmark discipline**: `current_waypoint` must follow current local evidence, not older waypoints, and stay aligned with `(Current)` in `waypoint_chain`. Resolve actual current space/side first. `subtask_landmark` should be the task-relevant concrete cue, not a broad room/space label or unrelated nearby object.
- **Chain/stop/output discipline**: `waypoint_chain` is the task-required order, not the executed Space Waypoint Chain order. Keep exactly one `(Current)` piece, advance only when the true stage endpoint is reached, and stop only at the exact required target; for landmark goals, require the correct target space plus the goal landmark near/current within about {arrival_near_m}m as the stop floor, never obstacle distance, and prefer a closer safe stop if available. The goal is not finished while any earlier task piece remains unfinished. Use last-position to judge whether the current stage has just started, is mid-segment, or is already passed, and treat `INITIAL POSITION` / last-position mainly as visited-history anchors; unless recovery is needed do not send `next_waypoint_direction` / `subtask_instruction` back toward them or any completed behind waypoint. Keep `next_waypoint_direction` / `subtask_landmark` / `subtask_instruction` aligned with the current task stage and do not invent future spaces/landmarks.
"""


def get_initial_planning_prompt(instruction: str, 
                               action_space: str) -> str:
    """
    获取初始规划提示词
    
    Args:
        instruction: 完整导航指令
        action_space: 动作空间描述
        
    Returns:
        格式化的提示词字符串
    """
    return INITIAL_PLANNING_PROMPT.format(
        instruction=instruction,
        action_space=action_space,
        obs_blocked_m=_fmt_threshold_m(OBS_BLOCKED_M),
        obs_risky_m=_fmt_threshold_m(OBS_RISKY_M),
        obs_open_m=_fmt_threshold_m(OBS_OPEN_M),
        arrival_near_m=_fmt_threshold_m(ARRIVAL_NEAR_M),
    )

def _get_verify_view_count(direction_names=None):
    provided_direction_names = [
        str(name).strip()
        for name in list(direction_names or [])
        if str(name or "").strip()
    ]
    view_count = len(provided_direction_names)
    return view_count if 0 < view_count < 12 else 12

def get_verification_replanning_prompt(instruction: str,
                                       subtask_destination: str,
                                       subtask_instruction: str,
                                       action_space: str,
                                       detected_landmarks: str = None,
                                       waypoint_summary: str = None,
                                       previous_subtask_landmark_summary: str = None,
                                       verify_replan_prompt_notice: str = None,
                                       direction_names: list = None) -> str:
    """
    获取验证和重规划提示词
    
    Args:
        instruction: 完整导航指令
        subtask_destination: 当前子任务目的地
        subtask_instruction: 当前子任务指令
        action_space: 动作空间描述
        detected_landmarks: 已检测到的landmark类别字符串
        waypoint_summary: 空间结构字符串
        previous_subtask_landmark_summary: 上一子任务landmark最终观测摘要
        verify_replan_prompt_notice: verify/replan 顶部附加提示
        direction_names: 本轮实际提供给模型的方向图名称列表
        
    Returns:
        格式化的提示词字符串
    """
    if not waypoint_summary:
        waypoint_summary = "Unavailable"
    if verify_replan_prompt_notice:
        verify_replan_prompt_notice_block = f"\n**Stuck Notice**: {verify_replan_prompt_notice.strip()}"
    else:
        verify_replan_prompt_notice_block = ""
    previous_subtask_landmark_summary = str(previous_subtask_landmark_summary or "").strip()
    previous_subtask_landmark_block = (
        f"- {previous_subtask_landmark_summary}"
        if previous_subtask_landmark_summary
        else ""
    )
    verify_view_count = _get_verify_view_count(direction_names)
    
    return VERIFICATION_REPLANNING_PROMPT.format(
        instruction=instruction,
        subtask_destination=subtask_destination,
        subtask_instruction=subtask_instruction,
        action_space=action_space,
        detected_landmarks=detected_landmarks,
        waypoint_summary=waypoint_summary,
        previous_subtask_landmark_summary=previous_subtask_landmark_summary,
        previous_subtask_landmark_block=previous_subtask_landmark_block,
        verify_replan_prompt_notice_block=verify_replan_prompt_notice_block,
        verify_view_count=verify_view_count,
        obs_blocked_m=_fmt_threshold_m(OBS_BLOCKED_M),
        obs_risky_m=_fmt_threshold_m(OBS_RISKY_M),
        obs_open_m=_fmt_threshold_m(OBS_OPEN_M),
        arrival_near_m=_fmt_threshold_m(ARRIVAL_NEAR_M),
    )
