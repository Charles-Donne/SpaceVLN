# SpaceVLN validated dependency snapshot.
#
# This file is synchronized against the working `spatial_agent` conda
# environment used to run the current system.
#
# Recommended usage:
#   1. create a fresh conda environment named `spacevln`
#   2. install CUDA-compatible PyTorch manually
#   3. install `habitat-sim==0.1.7` via conda
#   4. clone sibling source trees:
#        ../vlnce/habitat-lab
#        ../GroundingDINO
#   5. export CUDA_HOME
#   6. run `pip install -r requirements.txt`
#
# Notes:
# - `habitat-lab` and `GroundingDINO` are installed as editable sibling repos.
# - `segment_anything` is pinned to the git revision observed in the validated environment.
# - both `opencv-python` and `opencv-python-headless` are kept because both are
#   present in the validated runtime environment.

attr==0.3.2
attrs==25.3.0
dtw==1.4.0
dtw-python==1.4.2
fastdtw==0.3.4
gym==0.21.0
imageio==2.35.1
matplotlib==3.7.5
networkx==3.1
nltk==3.9.1
numpy==1.23.5
numpy-quaternion==2023.0.4
opencv-python==4.13.0.92
opencv-python-headless==4.10.0.84
Pillow==10.4.0
platformdirs==4.3.6
PyYAML==6.0.3
requests==2.32.4
scikit-image==0.19.0
scipy==1.10.1
segment_anything @ git+https://github.com/facebookresearch/segment-anything.git@dca509fe793f601edb92606367a655c15ac00fdf
supervision==0.4.0
timm==1.0.25
tomli==2.4.0
yacs==0.1.8
-e ../GroundingDINO
-e ../vlnce/habitat-lab

# CUDA-enabled PyTorch build used in the validated environment (install manually).
# torch==2.1.2+cu121
# torchvision==0.16.2+cu121
# torchaudio==2.1.2+cu121
